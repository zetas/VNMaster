# VNMaster Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a Python companion service to F95Checker that produces a weekly Discord digest of Ren'Py game updates with LLM-extracted changelog magnitude, plus a new-game discovery feed filtered by user tag preferences. Runs on macOS via launchd.

**Architecture:** A batch pipeline (`vnmaster digest`) reads three sources read-only (filesystem games folder, filesystem Ren'Py saves folder, F95Checker SQLite DB), joins them via fuzzy match, extracts changelog deltas via Claude Haiku, and posts a two-section embed bundle to Discord. A separate daemon (`vnmaster bot`) listens for reactions in the user's private channel and writes user-state back to vnmaster.db. All persistent state lives in one SQLite file at `~/Library/Application Support/VNMaster/vnmaster.db`.

**Tech Stack:** Python 3.12 · uv (deps + tool install) · pydantic v2 (config + types) · SQLAlchemy 2.x (DB) · alembic (migrations) · rapidfuzz (fuzzy match) · anthropic SDK (LLM) · discord.py 2.x (bot + gateway) · httpx (HTTP) · click (CLI) · pytest + pytest-asyncio + hypothesis (tests) · launchd (scheduling on macOS).

---

## Spec reference

The authoritative design is `docs/superpowers/specs/2026-05-19-vnmaster-design.md`. When this plan and the spec diverge, the spec wins — flag the divergence and stop. Re-read §3 for component boundaries, §4 for per-component behavior, §10 for what the human user must provide.

## File structure

```
VNMaster/
├── pyproject.toml                       # uv-managed project
├── README.md                            # short, install + first-run
├── .gitignore
├── docs/superpowers/
│   ├── specs/2026-05-19-vnmaster-design.md
│   └── plans/2026-05-19-vnmaster.md     # this file
├── scripts/
│   ├── plists/
│   │   ├── dev.vnmaster.weekly.plist.template
│   │   └── dev.vnmaster.bot.plist.template
│   └── test-fixtures/                   # synthetic Ren'Py game dirs, F95Checker DB
├── src/vnmaster/
│   ├── __init__.py                      # __version__
│   ├── __main__.py                      # `python -m vnmaster`
│   ├── cli.py                           # click entry: digest / bot / init / pair / status
│   ├── config.py                        # pydantic models for config.toml + secrets.toml
│   ├── logging_setup.py                 # rotating file + stderr handlers
│   ├── paths.py                         # XDG-style path resolution; macOS Application Support
│   ├── db/
│   │   ├── __init__.py
│   │   ├── engine.py                    # SQLAlchemy engine + session factory for vnmaster.db
│   │   ├── models.py                    # ORM classes: LibraryGame, Pairing, DiscoveryState, etc.
│   │   ├── migrations/                  # alembic
│   │   │   ├── env.py
│   │   │   ├── versions/
│   │   │   └── alembic.ini
│   │   └── ro_f95checker.py             # read-only F95Checker DB accessor + schema check
│   ├── scanners/
│   │   ├── __init__.py
│   │   ├── play_history.py              # walk ~/Library/RenPy/, emit PlayHistoryEntry
│   │   ├── disk.py                      # walk ~/Games/, detect Ren'Py installs, emit InstalledGame
│   │   └── types.py                     # pydantic dataclasses for scanner outputs
│   ├── matcher.py                       # fuzzy join: scanners + F95Checker rows → LibraryGame upserts
│   ├── llm/
│   │   ├── __init__.py
│   │   ├── changelog.py                 # Anthropic extractor with prompt caching + tool use
│   │   ├── prompts.py                   # system prompt + JSON schema
│   │   ├── fallback_regex.py            # regex-only extractor for budget-exhausted/offline runs
│   │   └── budget.py                    # monthly cost tracker
│   ├── discovery/
│   │   ├── __init__.py
│   │   ├── client.py                    # httpx wrapper for api.f95checker.dev
│   │   └── filter.py                    # pure filter pipeline (config-driven)
│   ├── magnitude.py                     # per-game score from extractions + config weights
│   ├── digest/
│   │   ├── __init__.py
│   │   ├── select.py                    # which library games + discovery rows to surface
│   │   ├── embeds.py                    # build Discord embed JSON
│   │   └── poster.py                    # webhook + bot-client reaction adder
│   ├── bot/
│   │   ├── __init__.py
│   │   ├── client.py                    # discord.py Bot subclass; gateway intents
│   │   ├── reactions.py                 # on_raw_reaction_add → apply_reaction()
│   │   ├── slash.py                     # /vnm tags, /vnm pair, /vnm status, /vnm digest
│   │   └── apply_reaction.py            # pure function: (thread_id, kind, action) → state delta
│   ├── pipeline.py                      # orchestrates one `vnmaster digest` run end-to-end
│   ├── init_wizard.py                   # `vnmaster init` interactive flow
│   ├── launchd.py                       # render + install/uninstall the two plists
│   └── _hooks.py                        # version-import dance for alembic
└── tests/
    ├── __init__.py
    ├── conftest.py                      # shared fixtures: tmp config, tmp db, fake renpy installs
    ├── fixtures/
    │   ├── renpy_game_valid/            # canonical fake game
    │   ├── renpy_game_malformed/
    │   ├── renpy_saves/                 # fake ~/Library/RenPy/
    │   ├── f95checker_db_11x.sqlite3    # canonical fixture
    │   └── changelogs/                  # 20 anonymized real changelog files + expected JSON
    ├── unit/
    │   ├── test_config.py
    │   ├── test_paths.py
    │   ├── test_scanners_play_history.py
    │   ├── test_scanners_disk.py
    │   ├── test_db_ro_f95checker.py
    │   ├── test_matcher.py
    │   ├── test_llm_fallback_regex.py
    │   ├── test_llm_budget.py
    │   ├── test_discovery_filter.py
    │   ├── test_magnitude.py
    │   ├── test_digest_select.py
    │   ├── test_digest_embeds.py
    │   └── test_apply_reaction.py
    ├── integration/
    │   ├── test_pipeline_e2e.py
    │   ├── test_llm_changelog_live.py   # gated by env VNMASTER_LIVE_LLM=1
    │   └── test_bot_reaction_flow.py
    └── golden/
        └── llm_extractions/             # cached LLM responses pinned per content hash
```

## Conventions

- **TDD always.** Every behavior gets a failing test before implementation. Run the test red, write code, run green, commit.
- **Commits are small.** One task = one or two commits. Commit message in conventional-commit-ish form (`feat:`, `test:`, `refactor:`, `chore:`).
- **Imports** use absolute paths from `vnmaster.` — no relative star imports.
- **Type hints required on every public function.** `mypy --strict` runs in CI eventually; we don't fail on it during initial development but write the hints.
- **Time** is always `int` epoch seconds at the DB boundary, `datetime` in Python code, never naive datetimes.
- **No print().** Everything goes through `logging_setup.get_logger(__name__)`.

---

## Phase 1 — Project foundation

### Task 1.1: Initialize uv project and dependency manifest

**Files:**
- Create: `pyproject.toml`
- Create: `src/vnmaster/__init__.py`
- Create: `src/vnmaster/__main__.py`
- Create: `tests/__init__.py`
- Create: `tests/conftest.py`

- [ ] **Step 1: Create `pyproject.toml`**

```toml
[project]
name = "vnmaster"
version = "0.1.0"
description = "Weekly Discord digest of Ren'Py VN updates with LLM-extracted changelog magnitude"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.40.0",
    "click>=8.1.7",
    "discord.py>=2.4.0",
    "httpx>=0.27.0",
    "pydantic>=2.7.0",
    "rapidfuzz>=3.9.0",
    "sqlalchemy>=2.0.30",
    "alembic>=1.13.0",
    "tomli>=2.0.1",
    "tomli-w>=1.0.0",
    "platformdirs>=4.2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.2.0",
    "pytest-asyncio>=0.23.0",
    "pytest-mock>=3.14.0",
    "hypothesis>=6.100.0",
    "freezegun>=1.5.0",
    "mypy>=1.10.0",
    "ruff>=0.4.0",
]

[project.scripts]
vnmaster = "vnmaster.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/vnmaster"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
asyncio_mode = "auto"
markers = [
    "live_llm: hits the Anthropic API; skipped unless VNMASTER_LIVE_LLM=1",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.mypy]
python_version = "3.12"
strict = true
```

- [ ] **Step 2: Create the package entrypoint files**

`src/vnmaster/__init__.py`:
```python
__version__ = "0.1.0"
```

`src/vnmaster/__main__.py`:
```python
from vnmaster.cli import main

if __name__ == "__main__":
    main()
```

`tests/__init__.py`: empty file.

`tests/conftest.py`:
```python
"""Shared pytest fixtures.

Concrete fixtures are added per-component as needed. This file exists so
pytest discovers tests under tests/ uniformly.
"""
```

- [ ] **Step 3: Install dependencies and verify the shell**

Run: `uv sync --all-extras`
Expected: dependency resolution succeeds; `.venv/` created.

Run: `uv run pytest -q`
Expected: `no tests ran in ...s` (no failures, just no tests yet).

- [ ] **Step 4: Commit**

```bash
git add pyproject.toml src/vnmaster/__init__.py src/vnmaster/__main__.py tests/__init__.py tests/conftest.py
git commit -m "chore: scaffold vnmaster project with uv + pytest"
```

---

### Task 1.2: Logging setup

**Files:**
- Create: `src/vnmaster/logging_setup.py`
- Create: `tests/unit/test_logging_setup.py`

- [ ] **Step 1: Write the failing test**

`tests/unit/test_logging_setup.py`:
```python
import logging
from pathlib import Path

from vnmaster.logging_setup import configure_logging, get_logger


def test_configure_logging_writes_to_file(tmp_path: Path) -> None:
    log_file = tmp_path / "vnmaster.log"
    configure_logging(log_file=log_file, level=logging.DEBUG)
    log = get_logger("vnmaster.test")
    log.info("hello world")
    for handler in logging.getLogger().handlers:
        handler.flush()
    contents = log_file.read_text()
    assert "hello world" in contents
    assert "vnmaster.test" in contents


def test_get_logger_returns_module_logger() -> None:
    log = get_logger("vnmaster.foo")
    assert log.name == "vnmaster.foo"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run pytest tests/unit/test_logging_setup.py -v`
Expected: ImportError or ModuleNotFoundError on `vnmaster.logging_setup`.

- [ ] **Step 3: Implement logging_setup.py**

```python
"""Project-wide logging configuration.

A single `configure_logging` call at the start of each entrypoint installs a
rotating file handler and a stderr handler. Module code uses `get_logger`.
"""
from __future__ import annotations

import logging
from logging.handlers import RotatingFileHandler
from pathlib import Path

_DEFAULT_FORMAT = "%(asctime)s %(levelname)-7s %(name)s | %(message)s"


def configure_logging(log_file: Path, level: int = logging.INFO) -> None:
    log_file.parent.mkdir(parents=True, exist_ok=True)
    root = logging.getLogger()
    root.setLevel(level)
    root.handlers.clear()

    formatter = logging.Formatter(_DEFAULT_FORMAT)

    file_handler = RotatingFileHandler(
        log_file, maxBytes=5_000_000, backupCount=3, encoding="utf-8"
    )
    file_handler.setFormatter(formatter)
    root.addHandler(file_handler)

    stderr_handler = logging.StreamHandler()
    stderr_handler.setFormatter(formatter)
    root.addHandler(stderr_handler)


def get_logger(name: str) -> logging.Logger:
    return logging.getLogger(name)
```

- [ ] **Step 4: Run the test, see green**

Run: `uv run pytest tests/unit/test_logging_setup.py -v`
Expected: 2 passed.

- [ ] **Step 5: Commit**

```bash
git add src/vnmaster/logging_setup.py tests/unit/test_logging_setup.py
git commit -m "feat: add rotating file + stderr logging setup"
```

---

### Task 1.3: Path resolution helpers

**Files:**
- Create: `src/vnmaster/paths.py`
- Create: `tests/unit/test_paths.py`

- [ ] **Step 1: Write the failing test**

`tests/unit/test_paths.py`:
```python
from pathlib import Path

from vnmaster.paths import VNMasterPaths


def test_default_paths_on_macos(monkeypatch, tmp_path: Path) -> None:
    monkeypatch.setenv("HOME", str(tmp_path))
    paths = VNMasterPaths.defaults_for_macos()
    assert paths.games_root == tmp_path / "Games"
    assert paths.renpy_saves_root == tmp_path / "Library" / "RenPy"
    assert paths.f95checker_db == (
        tmp_path / "Library" / "Application Support" / "F95Checker" / "db.sqlite3"
    )
    assert paths.vnmaster_db == (
        tmp_path / "Library" / "Application Support" / "VNMaster" / "vnmaster.db"
    )
    assert paths.config_dir == tmp_path / ".config" / "vnmaster"
    assert paths.log_dir == tmp_path / "Library" / "Logs" / "VNMaster"


def test_expand_user_handles_tildes(tmp_path: Path) -> None:
    p = VNMasterPaths.expand_user(Path("~/foo/bar"), home=tmp_path)
    assert p == tmp_path / "foo" / "bar"
```

- [ ] **Step 2: Run test, see it fail**

Run: `uv run pytest tests/unit/test_paths.py -v`
Expected: ImportError.

- [ ] **Step 3: Implement paths.py**

```python
"""Path resolution for VNMaster.

macOS-only for v1.0. All paths derive from $HOME.
"""
from __future__ import annotations

import os
from dataclasses import dataclass
from pathlib import Path


@dataclass(frozen=True)
class VNMasterPaths:
    games_root: Path
    renpy_saves_root: Path
    f95checker_db: Path
    vnmaster_db: Path
    config_dir: Path
    log_dir: Path

    @classmethod
    def defaults_for_macos(cls) -> "VNMasterPaths":
        home = Path(os.environ["HOME"])
        return cls(
            games_root=home / "Games",
            renpy_saves_root=home / "Library" / "RenPy",
            f95checker_db=home
            / "Library"
            / "Application Support"
            / "F95Checker"
            / "db.sqlite3",
            vnmaster_db=home
            / "Library"
            / "Application Support"
            / "VNMaster"
            / "vnmaster.db",
            config_dir=home / ".config" / "vnmaster",
            log_dir=home / "Library" / "Logs" / "VNMaster",
        )

    @staticmethod
    def expand_user(p: Path, home: Path | None = None) -> Path:
        home = home or Path(os.environ["HOME"])
        s = str(p)
        if s.startswith("~/"):
            return home / s[2:]
        if s == "~":
            return home
        return p
```

- [ ] **Step 4: Run test, see green**

Run: `uv run pytest tests/unit/test_paths.py -v`
Expected: 2 passed.

- [ ] **Step 5: Commit**

```bash
git add src/vnmaster/paths.py tests/unit/test_paths.py
git commit -m "feat: add VNMasterPaths defaults + tilde expansion"
```

---

### Task 1.4: CLI scaffolding

**Files:**
- Create: `src/vnmaster/cli.py`
- Create: `tests/unit/test_cli.py`

- [ ] **Step 1: Write the failing test**

`tests/unit/test_cli.py`:
```python
from click.testing import CliRunner

from vnmaster.cli import main


def test_cli_has_subcommands() -> None:
    runner = CliRunner()
    result = runner.invoke(main, ["--help"])
    assert result.exit_code == 0
    for sub in ["digest", "bot", "init", "pair", "status"]:
        assert sub in result.output


def test_digest_subcommand_exists() -> None:
    runner = CliRunner()
    result = runner.invoke(main, ["digest", "--help"])
    assert result.exit_code == 0
    assert "Run one digest pipeline now" in result.output
```

- [ ] **Step 2: Run test, see it fail**

Run: `uv run pytest tests/unit/test_cli.py -v`
Expected: ImportError.

- [ ] **Step 3: Implement cli.py with placeholder subcommands**

```python
"""CLI entrypoint. Each subcommand is a thin wrapper around a module function."""
from __future__ import annotations

import click


@click.group()
@click.version_option()
def main() -> None:
    """VNMaster — weekly Discord digest for F95Zone Ren'Py games."""


@main.command()
def digest() -> None:
    """Run one digest pipeline now and post to Discord."""
    click.echo("digest: not yet implemented (Phase 11 task)")


@main.command()
def bot() -> None:
    """Run the Discord reaction bot daemon in the foreground."""
    click.echo("bot: not yet implemented (Phase 13 task)")


@main.command()
def init() -> None:
    """Interactive first-run setup wizard."""
    click.echo("init: not yet implemented (Phase 15 task)")


@main.command()
@click.argument("name")
@click.argument("f95_url")
def pair(name: str, f95_url: str) -> None:
    """Manually pair a save dir / folder name to an F95 thread URL."""
    click.echo(f"pair: not yet implemented (will pair {name!r} → {f95_url})")


@main.command()
def status() -> None:
    """Print last digest run, llm cost MTD, unmatched count."""
    click.echo("status: not yet implemented")
```

- [ ] **Step 4: Run tests, see green**

Run: `uv run pytest tests/unit/test_cli.py -v`
Expected: 2 passed.

- [ ] **Step 5: Commit**

```bash
git add src/vnmaster/cli.py tests/unit/test_cli.py
git commit -m "feat: scaffold click CLI with placeholder subcommands"
```

---

## Phase 2 — Configuration and secrets

### Task 2.1: Config model

**Files:**
- Create: `src/vnmaster/config.py`
- Create: `tests/unit/test_config.py`
- Create: `tests/fixtures/configs/valid.toml`
- Create: `tests/fixtures/configs/missing_required.toml`

- [ ] **Step 1: Write the failing test**

`tests/fixtures/configs/valid.toml`:
```toml
[paths]
games_root = "~/Games"
renpy_saves_root = "~/Library/RenPy"
f95checker_db = "~/Library/Application Support/F95Checker/db.sqlite3"
vnmaster_db = "~/Library/Application Support/VNMaster/vnmaster.db"

[discord]
guild_id = "123456789"
channel_id = "987654321"
webhook_url = "https://discord.com/api/webhooks/foo/bar"

[anthropic]
model = "claude-haiku-4-5"
monthly_budget_usd = 5.0

[schedule]
cron = "0 9 * * SAT"

[discovery]
include_tags = ["corruption", "slow-burn"]
exclude_tags = ["ntr"]
max_suggestions_per_digest = 10
skip_window_weeks = 4
discovery_lookback_days = 7

[matching]
fuzzy_threshold = 90

[magnitude_score]
renders = 1.0
animations = 5.0
words_per_1k = 1.0
scenes = 50.0
new_locations = 20.0
new_characters = 10.0
bugfix_only_penalty = -100.0
```

`tests/fixtures/configs/missing_required.toml`:
```toml
[paths]
games_root = "~/Games"
# everything else missing
```

`tests/unit/test_config.py`:
```python
from pathlib import Path

import pytest

from vnmaster.config import Config, ConfigError, Secrets


FIXTURES = Path(__file__).parent.parent / "fixtures" / "configs"


def test_config_loads_valid_toml(monkeypatch, tmp_path: Path) -> None:
    monkeypatch.setenv("HOME", str(tmp_path))
    cfg = Config.load(FIXTURES / "valid.toml")
    assert cfg.paths.games_root == tmp_path / "Games"
    assert cfg.discord.guild_id == "123456789"
    assert cfg.anthropic.model == "claude-haiku-4-5"
    assert cfg.discovery.include_tags == ["corruption", "slow-burn"]
    assert cfg.matching.fuzzy_threshold == 90
    assert cfg.magnitude_score.renders == 1.0


def test_config_missing_required_field_raises(tmp_path: Path) -> None:
    with pytest.raises(ConfigError):
        Config.load(FIXTURES / "missing_required.toml")


def test_secrets_chmod_warning(tmp_path: Path) -> None:
    secrets_file = tmp_path / "secrets.toml"
    secrets_file.write_text(
        'discord_bot_token = "tok"\nanthropic_api_key = "key"\n'
    )
    secrets_file.chmod(0o644)
    with pytest.warns(UserWarning, match="permissions are too open"):
        Secrets.load(secrets_file)


def test_secrets_with_correct_permissions(tmp_path: Path) -> None:
    secrets_file = tmp_path / "secrets.toml"
    secrets_file.write_text(
        'discord_bot_token = "tok"\nanthropic_api_key = "key"\n'
    )
    secrets_file.chmod(0o600)
    s = Secrets.load(secrets_file)
    assert s.discord_bot_token == "tok"
    assert s.anthropic_api_key == "key"
```

- [ ] **Step 2: Run, see red**

Run: `uv run pytest tests/unit/test_config.py -v`
Expected: ImportError.

- [ ] **Step 3: Implement `config.py`**

```python
"""Strongly-typed configuration loaded from TOML files.

Two files:
  ~/.config/vnmaster/config.toml   — non-secret settings
  ~/.config/vnmaster/secrets.toml  — API keys/tokens; must be chmod 600
"""
from __future__ import annotations

import stat
import tomllib
import warnings
from pathlib import Path

from pydantic import BaseModel, Field, ValidationError, field_validator

from vnmaster.paths import VNMasterPaths


class ConfigError(Exception):
    """Raised when the config file cannot be parsed or is missing fields."""


class PathsConfig(BaseModel):
    games_root: Path
    renpy_saves_root: Path
    f95checker_db: Path
    vnmaster_db: Path

    @field_validator("*", mode="before")
    @classmethod
    def _expand(cls, v: object) -> object:
        if isinstance(v, str):
            return VNMasterPaths.expand_user(Path(v))
        return v


class DiscordConfig(BaseModel):
    guild_id: str
    channel_id: str
    webhook_url: str


class AnthropicConfig(BaseModel):
    model: str = "claude-haiku-4-5"
    monthly_budget_usd: float = Field(gt=0.0)


class ScheduleConfig(BaseModel):
    cron: str = "0 9 * * SAT"


class DiscoveryConfig(BaseModel):
    include_tags: list[str] = Field(default_factory=list)
    exclude_tags: list[str] = Field(default_factory=list)
    max_suggestions_per_digest: int = 10
    skip_window_weeks: int = 4
    discovery_lookback_days: int = 7


class MatchingConfig(BaseModel):
    fuzzy_threshold: int = Field(default=90, ge=0, le=100)


class MagnitudeScoreConfig(BaseModel):
    renders: float = 1.0
    animations: float = 5.0
    words_per_1k: float = 1.0
    scenes: float = 50.0
    new_locations: float = 20.0
    new_characters: float = 10.0
    bugfix_only_penalty: float = -100.0


class Config(BaseModel):
    paths: PathsConfig
    discord: DiscordConfig
    anthropic: AnthropicConfig
    schedule: ScheduleConfig = ScheduleConfig()
    discovery: DiscoveryConfig = DiscoveryConfig()
    matching: MatchingConfig = MatchingConfig()
    magnitude_score: MagnitudeScoreConfig = MagnitudeScoreConfig()

    @classmethod
    def load(cls, path: Path) -> "Config":
        try:
            raw = tomllib.loads(path.read_text())
            return cls.model_validate(raw)
        except (tomllib.TOMLDecodeError, ValidationError) as e:
            raise ConfigError(f"failed to load {path}: {e}") from e


class Secrets(BaseModel):
    discord_bot_token: str
    anthropic_api_key: str

    @classmethod
    def load(cls, path: Path) -> "Secrets":
        mode = path.stat().st_mode
        if mode & (stat.S_IRGRP | stat.S_IROTH | stat.S_IWGRP | stat.S_IWOTH):
            warnings.warn(
                f"{path} permissions are too open — chmod 600 recommended",
                UserWarning,
                stacklevel=2,
            )
        try:
            raw = tomllib.loads(path.read_text())
            return cls.model_validate(raw)
        except (tomllib.TOMLDecodeError, ValidationError) as e:
            raise ConfigError(f"failed to load secrets {path}: {e}") from e
```

- [ ] **Step 4: Run tests, see green**

Run: `uv run pytest tests/unit/test_config.py -v`
Expected: 4 passed.

- [ ] **Step 5: Commit**

```bash
git add src/vnmaster/config.py tests/unit/test_config.py tests/fixtures/configs/
git commit -m "feat: pydantic-based config + secrets loader with permission check"
```

---

## Phase 3 — Database schema and migrations

### Task 3.1: SQLAlchemy ORM models

**Files:**
- Create: `src/vnmaster/db/__init__.py`
- Create: `src/vnmaster/db/models.py`
- Create: `src/vnmaster/db/engine.py`
- Create: `tests/unit/test_db_models.py`

- [ ] **Step 1: Write the failing test**

`tests/unit/test_db_models.py`:
```python
import time
from pathlib import Path

from sqlalchemy import select

from vnmaster.db.engine import create_engine_for, session_scope
from vnmaster.db.models import (
    Base,
    ChangelogExtraction,
    DigestEntry,
    DigestRun,
    DiscoveryState,
    DiscoverySuggestion,
    LibraryGame,
    Pairing,
)


def _make_engine(tmp_path: Path):
    db_path = tmp_path / "test.db"
    engine = create_engine_for(db_path)
    Base.metadata.create_all(engine)
    return engine


def test_library_game_round_trip(tmp_path: Path) -> None:
    engine = _make_engine(tmp_path)
    now = int(time.time())
    with session_scope(engine) as s:
        s.add(
            LibraryGame(
                f95_thread_id=42,
                game_title="Eternum",
                save_dir_name="Eternum-1234567890",
                last_played_at=now,
                latest_upstream_version="0.7.0",
                created_at=now,
                updated_at=now,
            )
        )
    with session_scope(engine) as s:
        found = s.execute(
            select(LibraryGame).where(LibraryGame.f95_thread_id == 42)
        ).scalar_one()
        assert found.game_title == "Eternum"
        assert found.hidden == 0


def test_pairing_unique(tmp_path: Path) -> None:
    engine = _make_engine(tmp_path)
    now = int(time.time())
    with session_scope(engine) as s:
        s.add(Pairing(f95_thread_id=1, save_dir_name="A", confidence=1.0, paired_at=now))
    with session_scope(engine) as s:
        found = s.execute(select(Pairing)).scalar_one()
        assert found.f95_thread_id == 1


def test_changelog_extraction_dedup_by_hash(tmp_path: Path) -> None:
    engine = _make_engine(tmp_path)
    now = int(time.time())
    with session_scope(engine) as s:
        s.add(
            ChangelogExtraction(
                f95_thread_id=10,
                content_hash="abc123",
                extraction_method="llm",
                versions_json="[]",
                extracted_at=now,
            )
        )
    with session_scope(engine) as s:
        rows = s.execute(select(ChangelogExtraction)).scalars().all()
        assert len(rows) == 1


def test_digest_run_and_entries_link(tmp_path: Path) -> None:
    engine = _make_engine(tmp_path)
    now = int(time.time())
    with session_scope(engine) as s:
        run = DigestRun(run_at=now, updates_count=0, discovery_count=0, llm_calls=0, llm_cost_usd=0.0)
        s.add(run)
        s.flush()
        s.add(
            DigestEntry(
                run_id=run.id,
                discord_message_id="m1",
                embed_index=0,
                kind="update",
                f95_thread_id=42,
            )
        )
    with session_scope(engine) as s:
        e = s.execute(select(DigestEntry)).scalar_one()
        assert e.kind == "update"


def test_discovery_state_default_flags(tmp_path: Path) -> None:
    engine = _make_engine(tmp_path)
    now = int(time.time())
    with session_scope(engine) as s:
        s.add(DiscoveryState(f95_thread_id=99, updated_at=now))
    with session_scope(engine) as s:
        found = s.execute(select(DiscoveryState)).scalar_one()
        assert found.hidden == 0
        assert found.interested == 0


def test_discovery_suggestion_persistable(tmp_path: Path) -> None:
    engine = _make_engine(tmp_path)
    now = int(time.time())
    with session_scope(engine) as s:
        s.add(
            DiscoverySuggestion(
                f95_thread_id=99,
                title="Acolyte",
                posted_at=now,
                first_seen_at=now,
            )
        )
    with session_scope(engine) as s:
        found = s.execute(select(DiscoverySuggestion)).scalar_one()
        assert found.title == "Acolyte"
```

- [ ] **Step 2: Run, see red**

Run: `uv run pytest tests/unit/test_db_models.py -v`
Expected: ImportError.

- [ ] **Step 3: Implement `db/engine.py`**

`src/vnmaster/db/__init__.py`: empty file.

`src/vnmaster/db/engine.py`:
```python
"""SQLAlchemy engine + session factory for vnmaster.db."""
from __future__ import annotations

from contextlib import contextmanager
from pathlib import Path
from typing import Iterator

from sqlalchemy import Engine, create_engine
from sqlalchemy.orm import Session, sessionmaker


def create_engine_for(db_path: Path) -> Engine:
    db_path.parent.mkdir(parents=True, exist_ok=True)
    return create_engine(f"sqlite:///{db_path}", future=True)


def create_readonly_engine(db_path: Path) -> Engine:
    # SQLite uri form supports mode=ro
    return create_engine(f"sqlite:///{db_path}?mode=ro&uri=true", future=True)


@contextmanager
def session_scope(engine: Engine) -> Iterator[Session]:
    SessionLocal = sessionmaker(engine, expire_on_commit=False, future=True)
    session = SessionLocal()
    try:
        yield session
        session.commit()
    except Exception:
        session.rollback()
        raise
    finally:
        session.close()
```

- [ ] **Step 4: Implement `db/models.py`**

```python
"""ORM models for vnmaster.db. Schema mirrors spec §4.4, §4.5, §4.8."""
from __future__ import annotations

from sqlalchemy import (
    Float,
    ForeignKey,
    Integer,
    String,
    Text,
    UniqueConstraint,
)
from sqlalchemy.orm import DeclarativeBase, Mapped, mapped_column, relationship


class Base(DeclarativeBase):
    pass


class LibraryGame(Base):
    __tablename__ = "library_games"

    f95_thread_id: Mapped[int] = mapped_column(Integer, primary_key=True)
    game_title: Mapped[str] = mapped_column(String, nullable=False)
    # Play history (nullable: user may never have played)
    save_dir_name: Mapped[str | None] = mapped_column(String, nullable=True)
    last_played_at: Mapped[int | None] = mapped_column(Integer, nullable=True)
    first_played_at: Mapped[int | None] = mapped_column(Integer, nullable=True)
    save_count: Mapped[int | None] = mapped_column(Integer, nullable=True)
    total_save_size_bytes: Mapped[int | None] = mapped_column(Integer, nullable=True)
    # Currently installed (nullable: may not be on disk now)
    install_path: Mapped[str | None] = mapped_column(String, nullable=True)
    installed_version: Mapped[str | None] = mapped_column(String, nullable=True)
    disk_size_bytes: Mapped[int | None] = mapped_column(Integer, nullable=True)
    # F95Checker mirror
    latest_upstream_version: Mapped[str | None] = mapped_column(String, nullable=True)
    upstream_last_updated_at: Mapped[int | None] = mapped_column(Integer, nullable=True)
    upstream_thread_url: Mapped[str | None] = mapped_column(String, nullable=True)
    raw_changelog: Mapped[str | None] = mapped_column(Text, nullable=True)
    tags_json: Mapped[str | None] = mapped_column(Text, nullable=True)
    status: Mapped[str | None] = mapped_column(String, nullable=True)
    developer: Mapped[str | None] = mapped_column(String, nullable=True)
    image_url: Mapped[str | None] = mapped_column(String, nullable=True)
    # User reaction state
    hidden: Mapped[int] = mapped_column(Integer, default=0)
    interested: Mapped[int] = mapped_column(Integer, default=0)
    acknowledged_version: Mapped[str | None] = mapped_column(String, nullable=True)
    last_seen_in_digest_at: Mapped[int | None] = mapped_column(Integer, nullable=True)
    # Audit
    created_at: Mapped[int] = mapped_column(Integer, nullable=False)
    updated_at: Mapped[int] = mapped_column(Integer, nullable=False)


class Pairing(Base):
    __tablename__ = "pairings"

    f95_thread_id: Mapped[int] = mapped_column(Integer, primary_key=True)
    save_dir_name: Mapped[str | None] = mapped_column(String, nullable=True)
    folder_name: Mapped[str | None] = mapped_column(String, nullable=True)
    confidence: Mapped[float] = mapped_column(Float, nullable=False)
    paired_at: Mapped[int] = mapped_column(Integer, nullable=False)


class DiscoverySuggestion(Base):
    __tablename__ = "discovery_suggestions"

    f95_thread_id: Mapped[int] = mapped_column(Integer, primary_key=True)
    title: Mapped[str] = mapped_column(String, nullable=False)
    developer: Mapped[str | None] = mapped_column(String, nullable=True)
    posted_at: Mapped[int] = mapped_column(Integer, nullable=False)
    likes: Mapped[int | None] = mapped_column(Integer, nullable=True)
    tags_json: Mapped[str | None] = mapped_column(Text, nullable=True)
    short_description: Mapped[str | None] = mapped_column(Text, nullable=True)
    image_url: Mapped[str | None] = mapped_column(String, nullable=True)
    first_seen_at: Mapped[int] = mapped_column(Integer, nullable=False)
    last_seen_in_digest_at: Mapped[int | None] = mapped_column(Integer, nullable=True)


class DiscoveryState(Base):
    __tablename__ = "discovery_state"

    f95_thread_id: Mapped[int] = mapped_column(Integer, primary_key=True)
    hidden: Mapped[int] = mapped_column(Integer, default=0)
    skip_until: Mapped[int | None] = mapped_column(Integer, nullable=True)
    interested: Mapped[int] = mapped_column(Integer, default=0)
    updated_at: Mapped[int] = mapped_column(Integer, nullable=False)


class ChangelogExtraction(Base):
    __tablename__ = "changelog_extractions"
    __table_args__ = (UniqueConstraint("f95_thread_id", "content_hash"),)

    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    f95_thread_id: Mapped[int] = mapped_column(Integer, nullable=False)
    content_hash: Mapped[str] = mapped_column(String, nullable=False)
    extraction_method: Mapped[str] = mapped_column(String, nullable=False)
    versions_json: Mapped[str] = mapped_column(Text, nullable=False)
    extracted_at: Mapped[int] = mapped_column(Integer, nullable=False)


class DigestRun(Base):
    __tablename__ = "digest_runs"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    run_at: Mapped[int] = mapped_column(Integer, nullable=False)
    updates_count: Mapped[int] = mapped_column(Integer, nullable=False)
    discovery_count: Mapped[int] = mapped_column(Integer, nullable=False)
    llm_calls: Mapped[int] = mapped_column(Integer, nullable=False)
    llm_cost_usd: Mapped[float] = mapped_column(Float, nullable=False)

    entries: Mapped[list["DigestEntry"]] = relationship(back_populates="run")


class DigestEntry(Base):
    __tablename__ = "digest_entries"
    __table_args__ = (UniqueConstraint("discord_message_id", "embed_index"),)

    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    run_id: Mapped[int] = mapped_column(ForeignKey("digest_runs.id"), nullable=False)
    discord_message_id: Mapped[str] = mapped_column(String, nullable=False)
    embed_index: Mapped[int] = mapped_column(Integer, nullable=False)
    kind: Mapped[str] = mapped_column(String, nullable=False)  # 'update' | 'discovery'
    f95_thread_id: Mapped[int] = mapped_column(Integer, nullable=False)

    run: Mapped[DigestRun] = relationship(back_populates="entries")
```

- [ ] **Step 5: Run tests, see green**

Run: `uv run pytest tests/unit/test_db_models.py -v`
Expected: 6 passed.

- [ ] **Step 6: Commit**

```bash
git add src/vnmaster/db/ tests/unit/test_db_models.py
git commit -m "feat: SQLAlchemy ORM for vnmaster.db (library, discovery, digest tables)"
```

---

### Task 3.1b: Shared time helper

**Files:**
- Create: `src/vnmaster/clock.py`
- Create: `tests/unit/test_clock.py`

- [ ] **Step 1: Write the failing test**

`tests/unit/test_clock.py`:
```python
from datetime import datetime, timezone

from freezegun import freeze_time

from vnmaster.clock import Clock, SystemClock


def test_system_clock_returns_int_epoch() -> None:
    c = SystemClock()
    t = c.now_epoch()
    assert isinstance(t, int)
    assert t > 1_700_000_000


def test_system_clock_now_datetime_aware() -> None:
    c = SystemClock()
    dt = c.now()
    assert dt.tzinfo is not None


@freeze_time("2026-05-19T14:00:00+00:00")
def test_clock_is_freezable() -> None:
    c = SystemClock()
    assert c.now_epoch() == int(datetime(2026, 5, 19, 14, 0, tzinfo=timezone.utc).timestamp())


def test_clock_is_a_protocol() -> None:
    class FakeClock:
        def now(self) -> datetime:
            return datetime(2026, 1, 1, tzinfo=timezone.utc)

        def now_epoch(self) -> int:
            return 1735689600

    f: Clock = FakeClock()
    assert f.now_epoch() == 1735689600
```

- [ ] **Step 2: Run, see red**

Run: `uv run pytest tests/unit/test_clock.py -v`
Expected: ImportError.

- [ ] **Step 3: Implement clock.py**

```python
"""Time abstraction so tests can pin 'now' deterministically."""
from __future__ import annotations

from datetime import datetime, timezone
from typing import Protocol


class Clock(Protocol):
    def now(self) -> datetime: ...
    def now_epoch(self) -> int: ...


class SystemClock:
    def now(self) -> datetime:
        return datetime.now(tz=timezone.utc)

    def now_epoch(self) -> int:
        return int(self.now().timestamp())
```

- [ ] **Step 4: Run tests, see green**

Run: `uv run pytest tests/unit/test_clock.py -v`
Expected: 4 passed.

- [ ] **Step 5: Commit**

```bash
git add src/vnmaster/clock.py tests/unit/test_clock.py
git commit -m "feat: Clock protocol + SystemClock"
```

---

### Task 3.2: Alembic migration baseline

**Files:**
- Create: `src/vnmaster/db/migrations/alembic.ini`
- Create: `src/vnmaster/db/migrations/env.py`
- Create: `src/vnmaster/db/migrations/script.py.mako`
- Create: `src/vnmaster/db/migrations/versions/.gitkeep`
- Run: `uv run alembic revision --autogenerate -m "initial schema"` from project root

- [ ] **Step 1: Create alembic.ini**

`src/vnmaster/db/migrations/alembic.ini`:
```ini
[alembic]
script_location = src/vnmaster/db/migrations
prepend_sys_path = .

[loggers]
keys = root

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
```

- [ ] **Step 2: Create `env.py`**

```python
"""Alembic environment. Loads VNMaster's metadata for autogenerate."""
from __future__ import annotations

import os
from logging.config import fileConfig

from alembic import context
from sqlalchemy import engine_from_config, pool

from vnmaster.db.models import Base

config = context.config
if config.config_file_name is not None:
    fileConfig(config.config_file_name)

target_metadata = Base.metadata


def run_migrations_offline() -> None:
    url = os.environ.get("VNMASTER_DB_URL", "sqlite:///vnmaster.db")
    context.configure(
        url=url,
        target_metadata=target_metadata,
        literal_binds=True,
        dialect_opts={"paramstyle": "named"},
    )
    with context.begin_transaction():
        context.run_migrations()


def run_migrations_online() -> None:
    section = config.get_section(config.config_ini_section) or {}
    section["sqlalchemy.url"] = os.environ.get(
        "VNMASTER_DB_URL", "sqlite:///vnmaster.db"
    )
    connectable = engine_from_config(
        section,
        prefix="sqlalchemy.",
        poolclass=pool.NullPool,
    )
    with connectable.connect() as connection:
        context.configure(connection=connection, target_metadata=target_metadata)
        with context.begin_transaction():
            context.run_migrations()


if context.is_offline_mode():
    run_migrations_offline()
else:
    run_migrations_online()
```

- [ ] **Step 3: Create `script.py.mako`** (alembic template)

```python
"""${message}

Revision ID: ${up_revision}
Revises: ${down_revision | comma,n}
Create Date: ${create_date}
"""
from __future__ import annotations

from typing import Sequence, Union

from alembic import op
import sqlalchemy as sa
${imports if imports else ""}

revision: str = ${repr(up_revision)}
down_revision: Union[str, None] = ${repr(down_revision)}
branch_labels: Union[str, Sequence[str], None] = ${repr(branch_labels)}
depends_on: Union[str, Sequence[str], None] = ${repr(depends_on)}


def upgrade() -> None:
    ${upgrades if upgrades else "pass"}


def downgrade() -> None:
    ${downgrades if downgrades else "pass"}
```

- [ ] **Step 4: Generate the initial revision**

Run: `uv run alembic -c src/vnmaster/db/migrations/alembic.ini revision --autogenerate -m "initial schema"`
Expected: A new file appears under `src/vnmaster/db/migrations/versions/`.

- [ ] **Step 5: Test the migration applies cleanly**

`tests/integration/__init__.py`: empty file.

`tests/integration/test_migrations.py`:
```python
import os
import subprocess
import sys
from pathlib import Path

from sqlalchemy import create_engine, inspect


def test_alembic_upgrade_head_creates_all_tables(tmp_path: Path) -> None:
    db = tmp_path / "v.db"
    env = {**os.environ, "VNMASTER_DB_URL": f"sqlite:///{db}"}
    result = subprocess.run(
        [
            sys.executable,
            "-m",
            "alembic",
            "-c",
            "src/vnmaster/db/migrations/alembic.ini",
            "upgrade",
            "head",
        ],
        env=env,
        capture_output=True,
        text=True,
    )
    assert result.returncode == 0, result.stderr
    engine = create_engine(f"sqlite:///{db}")
    names = set(inspect(engine).get_table_names())
    assert {
        "library_games",
        "pairings",
        "discovery_suggestions",
        "discovery_state",
        "changelog_extractions",
        "digest_runs",
        "digest_entries",
    } <= names
```

Run: `uv run pytest tests/integration/test_migrations.py -v`
Expected: 1 passed.

- [ ] **Step 6: Commit**

```bash
git add src/vnmaster/db/migrations/ tests/integration/test_migrations.py tests/integration/__init__.py
git commit -m "feat: alembic baseline migration generating initial schema"
```

---

## Phase 4 — Play-history scanner

### Task 4.1: Scanner types

**Files:**
- Create: `src/vnmaster/scanners/__init__.py`
- Create: `src/vnmaster/scanners/types.py`

- [ ] **Step 1: Implement (no tests needed — pure pydantic types)**

`src/vnmaster/scanners/__init__.py`: empty.

`src/vnmaster/scanners/types.py`:
```python
"""Scanner output records. Pure pydantic dataclasses."""
from __future__ import annotations

from pathlib import Path

from pydantic import BaseModel


class PlayHistoryEntry(BaseModel):
    save_dir_name: str
    save_dir_path: Path
    last_played_at: int | None  # epoch seconds; None if no .save files
    first_played_at: int | None
    save_count: int
    total_save_size_bytes: int
    persistent_data_present: bool


class InstalledGame(BaseModel):
    folder_name: str
    install_path: Path
    installed_version: str  # "unknown" if undetectable
    save_dir_hint: str | None  # from config.save_directory parsing
    disk_size_bytes: int
    launcher_name: str  # e.g., "MyGame.app"
```

- [ ] **Step 2: Commit**

```bash
git add src/vnmaster/scanners/__init__.py src/vnmaster/scanners/types.py
git commit -m "feat: scanner output type definitions"
```

---

### Task 4.2: Play-history scanner

**Files:**
- Create: `src/vnmaster/scanners/play_history.py`
- Create: `tests/fixtures/renpy_saves/EternumGame-1234567890/` (with fake save files)
- Create: `tests/fixtures/renpy_saves/EmptyDir/` (no save files)
- Create: `tests/fixtures/renpy_saves/WithPersistent/` (has `persistent` file)
- Create: `tests/unit/test_scanners_play_history.py`

- [ ] **Step 1: Create fixture directories**

```bash
mkdir -p tests/fixtures/renpy_saves/EternumGame-1234567890
mkdir -p tests/fixtures/renpy_saves/EmptyDir
mkdir -p tests/fixtures/renpy_saves/WithPersistent
touch tests/fixtures/renpy_saves/EternumGame-1234567890/1-1.save
touch tests/fixtures/renpy_saves/EternumGame-1234567890/2-1.save
touch tests/fixtures/renpy_saves/EternumGame-1234567890/persistent
touch tests/fixtures/renpy_saves/WithPersistent/persistent
echo "1-1 save body" > tests/fixtures/renpy_saves/EternumGame-1234567890/1-1.save
```

- [ ] **Step 2: Write the failing test**

`tests/unit/test_scanners_play_history.py`:
```python
import os
import time
from pathlib import Path

from vnmaster.scanners.play_history import scan_play_history

FIXTURES = Path(__file__).parent.parent / "fixtures" / "renpy_saves"


def test_scan_finds_all_subdirs() -> None:
    entries = scan_play_history(FIXTURES)
    names = {e.save_dir_name for e in entries}
    assert {"EternumGame-1234567890", "EmptyDir", "WithPersistent"} == names


def test_scan_counts_save_files_correctly() -> None:
    entries = {e.save_dir_name: e for e in scan_play_history(FIXTURES)}
    assert entries["EternumGame-1234567890"].save_count == 2
    assert entries["EmptyDir"].save_count == 0


def test_scan_detects_persistent_flag() -> None:
    entries = {e.save_dir_name: e for e in scan_play_history(FIXTURES)}
    assert entries["EternumGame-1234567890"].persistent_data_present is True
    assert entries["WithPersistent"].persistent_data_present is True
    assert entries["EmptyDir"].persistent_data_present is False


def test_scan_last_played_uses_save_mtime(tmp_path: Path) -> None:
    sub = tmp_path / "MyGame-9"
    sub.mkdir()
    save_a = sub / "1-1.save"
    save_b = sub / "2-1.save"
    save_a.write_text("a")
    save_b.write_text("b")
    now = int(time.time())
    os.utime(save_a, (now - 1000, now - 1000))
    os.utime(save_b, (now - 50, now - 50))
    entries = scan_play_history(tmp_path)
    e = next(x for x in entries if x.save_dir_name == "MyGame-9")
    assert e.last_played_at == now - 50
    assert e.first_played_at == now - 1000


def test_scan_total_size_sums_files(tmp_path: Path) -> None:
    sub = tmp_path / "SizeTest-1"
    sub.mkdir()
    (sub / "1-1.save").write_bytes(b"x" * 100)
    (sub / "2-1.save").write_bytes(b"y" * 200)
    entries = scan_play_history(tmp_path)
    e = next(x for x in entries if x.save_dir_name == "SizeTest-1")
    assert e.total_save_size_bytes == 300


def test_scan_handles_missing_root(tmp_path: Path) -> None:
    entries = scan_play_history(tmp_path / "does_not_exist")
    assert entries == []
```

- [ ] **Step 3: Run, see red**

Run: `uv run pytest tests/unit/test_scanners_play_history.py -v`
Expected: ImportError.

- [ ] **Step 4: Implement `play_history.py`**

```python
"""Scan ~/Library/RenPy/ for save data.

Each subdirectory is treated as a played-game record, even if the game is no
longer installed on disk.
"""
from __future__ import annotations

from pathlib import Path

from vnmaster.logging_setup import get_logger
from vnmaster.scanners.types import PlayHistoryEntry

log = get_logger(__name__)


def scan_play_history(root: Path) -> list[PlayHistoryEntry]:
    if not root.exists() or not root.is_dir():
        return []

    out: list[PlayHistoryEntry] = []
    for child in sorted(root.iterdir()):
        if not child.is_dir():
            continue
        try:
            entry = _entry_for(child)
        except PermissionError as e:
            log.warning("permission denied scanning %s: %s", child, e)
            continue
        out.append(entry)
    return out


def _entry_for(dirpath: Path) -> PlayHistoryEntry:
    save_files = list(dirpath.glob("*.save"))
    persistent = dirpath / "persistent"

    last_played: int | None = None
    first_played: int | None = None
    total_size = 0

    if save_files:
        mtimes = [int(s.stat().st_mtime) for s in save_files]
        last_played = max(mtimes)
        first_played = min(mtimes)

    for f in dirpath.iterdir():
        if f.is_file():
            try:
                total_size += f.stat().st_size
            except OSError:
                continue

    return PlayHistoryEntry(
        save_dir_name=dirpath.name,
        save_dir_path=dirpath,
        last_played_at=last_played,
        first_played_at=first_played,
        save_count=len(save_files),
        total_save_size_bytes=total_size,
        persistent_data_present=persistent.exists(),
    )
```

- [ ] **Step 5: Run tests, see green**

Run: `uv run pytest tests/unit/test_scanners_play_history.py -v`
Expected: 6 passed.

- [ ] **Step 6: Commit**

```bash
git add src/vnmaster/scanners/play_history.py tests/unit/test_scanners_play_history.py tests/fixtures/renpy_saves/
git commit -m "feat: play-history scanner for ~/Library/RenPy/"
```

---

## Phase 5 — Disk scanner

### Task 5.1: Ren'Py install detection

**Files:**
- Create: `src/vnmaster/scanners/disk.py`
- Create: `tests/fixtures/renpy_games/EternumGame-0.5.2-pc/` with `renpy/`, `game/`, `MyGame.app`, `game/options.rpy`
- Create: `tests/fixtures/renpy_games/NotARenpyGame/` (just a folder, no markers)
- Create: `tests/fixtures/renpy_games/WeirdName/` (Ren'Py structure but folder name has no version)
- Create: `tests/unit/test_scanners_disk.py`

- [ ] **Step 1: Build fixtures**

```bash
mkdir -p tests/fixtures/renpy_games/EternumGame-0.5.2-pc/renpy
mkdir -p tests/fixtures/renpy_games/EternumGame-0.5.2-pc/game
mkdir -p tests/fixtures/renpy_games/EternumGame-0.5.2-pc/MyGame.app
mkdir -p tests/fixtures/renpy_games/NotARenpyGame
mkdir -p tests/fixtures/renpy_games/WeirdName/renpy
mkdir -p tests/fixtures/renpy_games/WeirdName/game
touch tests/fixtures/renpy_games/WeirdName/run.sh
chmod +x tests/fixtures/renpy_games/WeirdName/run.sh
```

`tests/fixtures/renpy_games/EternumGame-0.5.2-pc/game/options.rpy`:
```python
define config.name = "Eternum"
define config.version = "0.5.2"
define config.save_directory = "Eternum-1234567890"
```

`tests/fixtures/renpy_games/WeirdName/game/options.rpy`:
```python
define config.version = "1.0.0a"
```

- [ ] **Step 2: Write the failing test**

`tests/unit/test_scanners_disk.py`:
```python
from pathlib import Path

from vnmaster.scanners.disk import scan_disk

FIXTURES = Path(__file__).parent.parent / "fixtures" / "renpy_games"


def test_scan_finds_renpy_games_only() -> None:
    games = scan_disk(FIXTURES)
    names = {g.folder_name for g in games}
    assert "EternumGame-0.5.2-pc" in names
    assert "WeirdName" in names
    assert "NotARenpyGame" not in names


def test_scan_extracts_version_from_folder_name() -> None:
    games = {g.folder_name: g for g in scan_disk(FIXTURES)}
    assert games["EternumGame-0.5.2-pc"].installed_version == "0.5.2"


def test_scan_falls_back_to_options_rpy_for_version() -> None:
    games = {g.folder_name: g for g in scan_disk(FIXTURES)}
    assert games["WeirdName"].installed_version == "1.0.0a"


def test_scan_extracts_save_dir_hint_from_options_rpy() -> None:
    games = {g.folder_name: g for g in scan_disk(FIXTURES)}
    assert games["EternumGame-0.5.2-pc"].save_dir_hint == "Eternum-1234567890"


def test_scan_marks_unknown_version_when_no_signals(tmp_path: Path) -> None:
    sub = tmp_path / "Mystery"
    (sub / "renpy").mkdir(parents=True)
    (sub / "game").mkdir()
    (sub / "Mystery.sh").touch()
    games = scan_disk(tmp_path)
    g = next(x for x in games if x.folder_name == "Mystery")
    assert g.installed_version == "unknown"


def test_scan_records_launcher_name() -> None:
    games = {g.folder_name: g for g in scan_disk(FIXTURES)}
    assert games["EternumGame-0.5.2-pc"].launcher_name == "MyGame.app"
    assert games["WeirdName"].launcher_name == "run.sh"


def test_scan_handles_missing_root(tmp_path: Path) -> None:
    assert scan_disk(tmp_path / "nope") == []
```

- [ ] **Step 3: Run, see red**

Run: `uv run pytest tests/unit/test_scanners_disk.py -v`
Expected: ImportError.

- [ ] **Step 4: Implement disk.py**

```python
"""Scan ~/Games/ for installed Ren'Py games.

A folder is a Ren'Py install if it contains:
  - renpy/ subdir
  - game/ subdir
  - a launcher: *.app, *.sh, or *.exe
"""
from __future__ import annotations

import re
from pathlib import Path

from vnmaster.logging_setup import get_logger
from vnmaster.scanners.types import InstalledGame

log = get_logger(__name__)

_VERSION_FROM_FOLDER_RE = re.compile(r"[-_ ](\d+(?:\.\d+)+[a-z]?)\b")
_CONFIG_VERSION_RE = re.compile(r'config\.version\s*=\s*"([^"]+)"')
_SAVE_DIR_RE = re.compile(r'config\.save_directory\s*=\s*"([^"]+)"')


def scan_disk(root: Path) -> list[InstalledGame]:
    if not root.exists() or not root.is_dir():
        return []

    out: list[InstalledGame] = []
    for child in sorted(root.iterdir()):
        if not child.is_dir():
            continue
        if not _looks_like_renpy(child):
            continue
        try:
            out.append(_game_for(child))
        except (PermissionError, OSError) as e:
            log.warning("could not scan %s: %s", child, e)
    return out


def _looks_like_renpy(dirpath: Path) -> bool:
    return (
        (dirpath / "renpy").is_dir()
        and (dirpath / "game").is_dir()
        and _find_launcher(dirpath) is not None
    )


def _find_launcher(dirpath: Path) -> Path | None:
    for f in dirpath.iterdir():
        name = f.name
        if name.endswith(".app") or name.endswith(".sh") or name.endswith(".exe"):
            return f
    return None


def _game_for(dirpath: Path) -> InstalledGame:
    options = dirpath / "game" / "options.rpy"
    options_text = options.read_text(errors="ignore") if options.exists() else ""

    version = _extract_version(dirpath.name, options_text)
    save_hint = _extract_save_dir_hint(options_text)
    launcher = _find_launcher(dirpath)
    assert launcher is not None, "should have been caught by _looks_like_renpy"

    return InstalledGame(
        folder_name=dirpath.name,
        install_path=dirpath,
        installed_version=version,
        save_dir_hint=save_hint,
        disk_size_bytes=_dir_size(dirpath),
        launcher_name=launcher.name,
    )


def _extract_version(folder_name: str, options_text: str) -> str:
    m = _VERSION_FROM_FOLDER_RE.search(folder_name)
    if m:
        return m.group(1)
    m = _CONFIG_VERSION_RE.search(options_text)
    if m:
        return m.group(1)
    return "unknown"


def _extract_save_dir_hint(options_text: str) -> str | None:
    m = _SAVE_DIR_RE.search(options_text)
    return m.group(1) if m else None


def _dir_size(dirpath: Path) -> int:
    total = 0
    for p in dirpath.rglob("*"):
        if p.is_file():
            try:
                total += p.stat().st_size
            except OSError:
                continue
    return total
```

- [ ] **Step 5: Run tests, see green**

Run: `uv run pytest tests/unit/test_scanners_disk.py -v`
Expected: 7 passed.

- [ ] **Step 6: Commit**

```bash
git add src/vnmaster/scanners/disk.py tests/unit/test_scanners_disk.py tests/fixtures/renpy_games/
git commit -m "feat: disk scanner for installed Ren'Py games with version + save_dir hints"
```

---

## Phase 6 — F95Checker read-only DB reader

### Task 6.1: Schema check and game query

**Files:**
- Create: `src/vnmaster/db/ro_f95checker.py`
- Create: `tests/fixtures/f95checker/build_fixture.py` (one-time script committed alongside the fixture it produces)
- Create: `tests/fixtures/f95checker/db_11x.sqlite3`
- Create: `tests/unit/test_db_ro_f95checker.py`

- [ ] **Step 1: Build the fixture DB**

`tests/fixtures/f95checker/build_fixture.py`:
```python
"""Generates a tiny F95Checker-shaped SQLite for tests.

Run once; commit the resulting db_11x.sqlite3. Re-run if the F95Checker
schema changes.
"""
from __future__ import annotations

import sqlite3
import time
from pathlib import Path

HERE = Path(__file__).parent
DB = HERE / "db_11x.sqlite3"


def build() -> None:
    if DB.exists():
        DB.unlink()
    conn = sqlite3.connect(DB)
    cur = conn.cursor()
    cur.execute(
        """
        CREATE TABLE games (
            id INTEGER PRIMARY KEY,
            name TEXT,
            version TEXT,
            developer TEXT,
            engine TEXT,
            status TEXT,
            last_updated INTEGER,
            changelog TEXT,
            description TEXT,
            image_url TEXT,
            tags TEXT,
            executable TEXT,
            archived INTEGER,
            rating INTEGER
        )
        """
    )
    now = int(time.time())
    rows = [
        (
            42, "Eternum", "0.7.0", "Caribdis", "Ren'Py", "ongoing",
            now, "v0.7.0\n- 800 new renders", "Sci-fi VN", "img.png",
            "corruption,slow-burn", "/Users/x/Games/Eternum/MyGame.app", 0, 4,
        ),
        (
            99, "Summer's Gone", "0.10", "Oceanlab", "Ren'Py", "ongoing",
            now - 86400, "v0.10\n- minor", "Adult VN", "img2.png",
            "slow-burn,romance", None, 0, 5,
        ),
    ]
    cur.executemany(
        "INSERT INTO games VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)",
        rows,
    )
    conn.commit()
    conn.close()


if __name__ == "__main__":
    build()
```

Run once: `uv run python tests/fixtures/f95checker/build_fixture.py`
Result: `tests/fixtures/f95checker/db_11x.sqlite3` is created.

- [ ] **Step 2: Write the failing test**

`tests/unit/test_db_ro_f95checker.py`:
```python
import sqlite3
from pathlib import Path

import pytest

from vnmaster.db.ro_f95checker import F95CheckerDB, SchemaMismatchError

FIXTURE = Path(__file__).parent.parent / "fixtures" / "f95checker" / "db_11x.sqlite3"


def test_open_and_schema_check_passes() -> None:
    db = F95CheckerDB.open(FIXTURE)
    db.check_schema()  # no raise


def test_iter_all_games_returns_rows() -> None:
    db = F95CheckerDB.open(FIXTURE)
    games = list(db.iter_all_games())
    assert len(games) == 2
    e = next(g for g in games if g.id == 42)
    assert e.name == "Eternum"
    assert e.version == "0.7.0"
    assert e.executable == "/Users/x/Games/Eternum/MyGame.app"
    assert e.tags == ["corruption", "slow-burn"]


def test_iter_games_updated_since_filter() -> None:
    db = F95CheckerDB.open(FIXTURE)
    fresh = list(db.iter_games_updated_since(0))
    assert len(fresh) == 2
    none = list(db.iter_games_updated_since(2 * 10**10))
    assert none == []


def test_schema_mismatch_raises(tmp_path: Path) -> None:
    bad = tmp_path / "bad.sqlite3"
    conn = sqlite3.connect(bad)
    conn.execute("CREATE TABLE games (id INTEGER, name TEXT)")
    conn.commit()
    conn.close()
    db = F95CheckerDB.open(bad)
    with pytest.raises(SchemaMismatchError):
        db.check_schema()
```

- [ ] **Step 3: Run, see red**

Run: `uv run pytest tests/unit/test_db_ro_f95checker.py -v`
Expected: ImportError.

- [ ] **Step 4: Implement ro_f95checker.py**

```python
"""Read-only accessor for F95Checker's local SQLite DB.

Never writes. Verifies a known-good schema on open. Iterating games returns
typed pydantic records.
"""
from __future__ import annotations

from dataclasses import dataclass
from pathlib import Path
from typing import Iterator

from sqlalchemy import Engine, inspect, text

from vnmaster.db.engine import create_readonly_engine

REQUIRED_COLUMNS = {
    "id", "name", "version", "developer", "engine", "status",
    "last_updated", "changelog", "description", "image_url",
    "tags", "executable", "archived", "rating",
}


class SchemaMismatchError(Exception):
    pass


@dataclass(frozen=True)
class F95CheckerGame:
    id: int
    name: str
    version: str | None
    developer: str | None
    engine: str | None
    status: str | None
    last_updated: int | None
    changelog: str | None
    description: str | None
    image_url: str | None
    tags: list[str]
    executable: str | None
    archived: int
    rating: int | None


class F95CheckerDB:
    def __init__(self, engine: Engine) -> None:
        self._engine = engine

    @classmethod
    def open(cls, path: Path) -> "F95CheckerDB":
        return cls(create_readonly_engine(path))

    def check_schema(self) -> None:
        insp = inspect(self._engine)
        if "games" not in insp.get_table_names():
            raise SchemaMismatchError("F95Checker DB missing 'games' table")
        cols = {c["name"] for c in insp.get_columns("games")}
        missing = REQUIRED_COLUMNS - cols
        if missing:
            raise SchemaMismatchError(
                f"F95Checker DB schema missing required columns: {sorted(missing)}"
            )

    def iter_all_games(self) -> Iterator[F95CheckerGame]:
        sql = text(
            "SELECT id, name, version, developer, engine, status, last_updated, "
            "changelog, description, image_url, tags, executable, archived, rating "
            "FROM games"
        )
        with self._engine.connect() as conn:
            for row in conn.execute(sql):
                yield _row_to_game(row)

    def iter_games_updated_since(self, since_epoch: int) -> Iterator[F95CheckerGame]:
        sql = text(
            "SELECT id, name, version, developer, engine, status, last_updated, "
            "changelog, description, image_url, tags, executable, archived, rating "
            "FROM games WHERE last_updated > :since"
        )
        with self._engine.connect() as conn:
            for row in conn.execute(sql, {"since": since_epoch}):
                yield _row_to_game(row)


def _row_to_game(row) -> F95CheckerGame:
    return F95CheckerGame(
        id=row[0],
        name=row[1],
        version=row[2],
        developer=row[3],
        engine=row[4],
        status=row[5],
        last_updated=row[6],
        changelog=row[7],
        description=row[8],
        image_url=row[9],
        tags=[t.strip() for t in (row[10] or "").split(",") if t.strip()],
        executable=row[11],
        archived=row[12] or 0,
        rating=row[13],
    )
```

- [ ] **Step 5: Run tests, see green**

Run: `uv run pytest tests/unit/test_db_ro_f95checker.py -v`
Expected: 4 passed.

- [ ] **Step 6: Commit**

```bash
git add src/vnmaster/db/ro_f95checker.py tests/unit/test_db_ro_f95checker.py tests/fixtures/f95checker/
git commit -m "feat: read-only F95Checker DB accessor with schema check"
```

---

## Phase 7 — Library matcher

### Task 7.1: Fuzzy matcher pure function

**Files:**
- Create: `src/vnmaster/matcher.py`
- Create: `tests/unit/test_matcher.py`

- [ ] **Step 1: Write the failing test**

`tests/unit/test_matcher.py`:
```python
from pathlib import Path

from vnmaster.db.ro_f95checker import F95CheckerGame
from vnmaster.matcher import MatchResult, match_library
from vnmaster.scanners.types import InstalledGame, PlayHistoryEntry


def _save(name: str, last_played: int = 1700_000_000) -> PlayHistoryEntry:
    return PlayHistoryEntry(
        save_dir_name=name,
        save_dir_path=Path("/tmp") / name,
        last_played_at=last_played,
        first_played_at=last_played - 100,
        save_count=2,
        total_save_size_bytes=100,
        persistent_data_present=True,
    )


def _disk(name: str, version: str = "0.5.0", hint: str | None = None) -> InstalledGame:
    return InstalledGame(
        folder_name=name,
        install_path=Path("/tmp/Games") / name,
        installed_version=version,
        save_dir_hint=hint,
        disk_size_bytes=1_000_000,
        launcher_name="MyGame.app",
    )


def _f95(thread_id: int, name: str, executable: str | None = None) -> F95CheckerGame:
    return F95CheckerGame(
        id=thread_id, name=name, version="0.7.0", developer="Dev", engine="Ren'Py",
        status="ongoing", last_updated=1700_000_001, changelog="", description="",
        image_url="", tags=["corruption"], executable=executable, archived=0, rating=None,
    )


def test_disk_matches_by_executable_path() -> None:
    install_path = Path("/Users/x/Games/Eternum")
    disk_entry = _disk("Eternum-0.5.2-pc")
    disk_entry = disk_entry.model_copy(update={"install_path": install_path})
    f95 = _f95(42, "Eternum", executable=str(install_path / "MyGame.app"))
    result = match_library(
        play_history=[],
        installed=[disk_entry],
        f95_rows=[f95],
        cached_pairings={},
        fuzzy_threshold=90,
    )
    assert result.matches[0].f95_thread_id == 42
    assert result.matches[0].install_path == install_path


def test_disk_falls_back_to_fuzzy_match_on_name() -> None:
    disk_entry = _disk("Eternum-0.5.2-pc")
    f95 = _f95(42, "Eternum")
    result = match_library(
        play_history=[], installed=[disk_entry], f95_rows=[f95],
        cached_pairings={}, fuzzy_threshold=90,
    )
    assert len(result.matches) == 1
    assert result.matches[0].f95_thread_id == 42


def test_save_dir_fuzzy_matches_when_disk_hint_missing() -> None:
    save = _save("Eternum-1234567890")
    f95 = _f95(42, "Eternum")
    result = match_library(
        play_history=[save], installed=[], f95_rows=[f95],
        cached_pairings={}, fuzzy_threshold=90,
    )
    assert result.matches[0].f95_thread_id == 42
    assert result.matches[0].save_dir_name == "Eternum-1234567890"


def test_disk_save_dir_hint_binds_to_play_history() -> None:
    save = _save("SecretSave-1234567890")
    disk_entry = _disk("MyGame", hint="SecretSave-1234567890")
    f95 = _f95(42, "MyGame")
    result = match_library(
        play_history=[save], installed=[disk_entry], f95_rows=[f95],
        cached_pairings={}, fuzzy_threshold=90,
    )
    m = result.matches[0]
    assert m.f95_thread_id == 42
    assert m.save_dir_name == "SecretSave-1234567890"
    assert m.installed_version == "0.5.0"


def test_unmatched_when_below_threshold() -> None:
    disk_entry = _disk("CompletelyDifferentName")
    f95 = _f95(42, "Eternum")
    result = match_library(
        play_history=[], installed=[disk_entry], f95_rows=[f95],
        cached_pairings={}, fuzzy_threshold=90,
    )
    assert result.matches == []
    assert len(result.unmatched_installed) == 1


def test_cached_pairing_overrides_fuzzy_match() -> None:
    save = _save("BoringSaveName")
    f95_a = _f95(10, "Game A")
    f95_b = _f95(20, "Game B")
    result = match_library(
        play_history=[save], installed=[], f95_rows=[f95_a, f95_b],
        cached_pairings={"BoringSaveName": 20}, fuzzy_threshold=90,
    )
    assert result.matches[0].f95_thread_id == 20
```

- [ ] **Step 2: Run, see red**

Run: `uv run pytest tests/unit/test_matcher.py -v`
Expected: ImportError.

- [ ] **Step 3: Implement matcher.py**

```python
"""Join scanner output + F95Checker rows into LibraryGame upserts.

Pure function — takes lists in, returns a MatchResult out. No DB access.
"""
from __future__ import annotations

from dataclasses import dataclass
from pathlib import Path

from rapidfuzz import fuzz, process

from vnmaster.db.ro_f95checker import F95CheckerGame
from vnmaster.scanners.types import InstalledGame, PlayHistoryEntry


@dataclass(frozen=True)
class LibraryMatch:
    f95_thread_id: int
    game_title: str
    save_dir_name: str | None
    last_played_at: int | None
    first_played_at: int | None
    save_count: int | None
    total_save_size_bytes: int | None
    install_path: Path | None
    installed_version: str | None
    disk_size_bytes: int | None


@dataclass(frozen=True)
class MatchResult:
    matches: list[LibraryMatch]
    unmatched_installed: list[InstalledGame]
    unmatched_play_history: list[PlayHistoryEntry]


def match_library(
    play_history: list[PlayHistoryEntry],
    installed: list[InstalledGame],
    f95_rows: list[F95CheckerGame],
    cached_pairings: dict[str, int],
    fuzzy_threshold: int,
) -> MatchResult:
    f95_by_id = {f.id: f for f in f95_rows}
    f95_by_name_for_fuzzy: dict[str, int] = {f.name: f.id for f in f95_rows}

    # Index disk entries by save_dir_hint for cross-binding.
    disk_by_hint: dict[str, InstalledGame] = {}
    disk_remaining = list(installed)
    for d in installed:
        if d.save_dir_hint:
            disk_by_hint[d.save_dir_hint] = d

    # Resolve each save dir to an F95 thread.
    save_to_thread: dict[str, int] = {}
    unmatched_saves: list[PlayHistoryEntry] = []
    for save in play_history:
        thread_id = _resolve(save.save_dir_name, f95_by_name_for_fuzzy,
                              cached_pairings, fuzzy_threshold)
        if thread_id is not None:
            save_to_thread[save.save_dir_name] = thread_id
        else:
            unmatched_saves.append(save)

    # Resolve each disk entry to an F95 thread (executable path beats fuzzy).
    disk_to_thread: dict[str, int] = {}
    unmatched_disk: list[InstalledGame] = []
    for d in disk_remaining:
        thread_id = _resolve_disk(d, f95_rows, cached_pairings, fuzzy_threshold)
        if thread_id is not None:
            disk_to_thread[d.folder_name] = thread_id
        else:
            unmatched_disk.append(d)

    # Build a per-thread merged record.
    by_thread: dict[int, LibraryMatch] = {}

    for save in play_history:
        tid = save_to_thread.get(save.save_dir_name)
        if tid is None:
            continue
        f95 = f95_by_id[tid]
        by_thread[tid] = LibraryMatch(
            f95_thread_id=tid, game_title=f95.name,
            save_dir_name=save.save_dir_name,
            last_played_at=save.last_played_at,
            first_played_at=save.first_played_at,
            save_count=save.save_count,
            total_save_size_bytes=save.total_save_size_bytes,
            install_path=None, installed_version=None, disk_size_bytes=None,
        )

    for d in installed:
        tid = disk_to_thread.get(d.folder_name)
        if tid is None:
            continue
        # Cross-bind by save_dir_hint when present.
        save_name = d.save_dir_hint
        existing = by_thread.get(tid)
        if existing is not None:
            by_thread[tid] = LibraryMatch(
                **{**existing.__dict__, "install_path": d.install_path,
                   "installed_version": d.installed_version,
                   "disk_size_bytes": d.disk_size_bytes}
            )
        else:
            by_thread[tid] = LibraryMatch(
                f95_thread_id=tid, game_title=f95_by_id[tid].name,
                save_dir_name=save_name, last_played_at=None,
                first_played_at=None, save_count=None, total_save_size_bytes=None,
                install_path=d.install_path,
                installed_version=d.installed_version,
                disk_size_bytes=d.disk_size_bytes,
            )

    return MatchResult(
        matches=list(by_thread.values()),
        unmatched_installed=unmatched_disk,
        unmatched_play_history=unmatched_saves,
    )


def _resolve(
    key: str,
    f95_by_name: dict[str, int],
    cached_pairings: dict[str, int],
    threshold: int,
) -> int | None:
    if key in cached_pairings:
        return cached_pairings[key]
    if not f95_by_name:
        return None
    best = process.extractOne(key, list(f95_by_name.keys()), scorer=fuzz.token_set_ratio)
    if best is None:
        return None
    name, score, _ = best
    if score >= threshold:
        return f95_by_name[name]
    return None


def _resolve_disk(
    d: InstalledGame,
    f95_rows: list[F95CheckerGame],
    cached_pairings: dict[str, int],
    threshold: int,
) -> int | None:
    if d.folder_name in cached_pairings:
        return cached_pairings[d.folder_name]
    # Executable-path match
    install_str = str(d.install_path)
    for f in f95_rows:
        if f.executable and (
            f.executable.startswith(install_str) or install_str.startswith(str(Path(f.executable).parent))
        ):
            return f.id
    # Fuzzy fallback
    return _resolve(
        d.folder_name, {f.name: f.id for f in f95_rows}, cached_pairings, threshold
    )
```

- [ ] **Step 4: Run tests, see green**

Run: `uv run pytest tests/unit/test_matcher.py -v`
Expected: 6 passed.

- [ ] **Step 5: Commit**

```bash
git add src/vnmaster/matcher.py tests/unit/test_matcher.py
git commit -m "feat: library matcher joining scanners + F95Checker via fuzzy + cached pairings"
```

---

## Phase 8 — LLM changelog magnitude extractor

### Task 8.1: Regex fallback extractor

**Files:**
- Create: `src/vnmaster/llm/__init__.py`
- Create: `src/vnmaster/llm/fallback_regex.py`
- Create: `tests/unit/test_llm_fallback_regex.py`

- [ ] **Step 1: Write the failing test**

`tests/unit/test_llm_fallback_regex.py`:
```python
from vnmaster.llm.fallback_regex import extract_with_regex


def test_extracts_renders_animations_words() -> None:
    text = """
    v0.7.0
    - 800 new renders
    - 35 animations
    - 12,000 lines of dialogue
    """
    result = extract_with_regex(text)
    assert len(result["versions"]) == 1
    v = result["versions"][0]
    assert v["version"] == "0.7.0"
    assert v["renders"] == 800
    assert v["animations"] == 35
    assert v["words"] == 12000
    assert v["bugfix_only"] is False


def test_marks_bugfix_only_when_no_content_changes() -> None:
    text = "v0.5.3\n- Bug fixes\n- Performance improvements"
    result = extract_with_regex(text)
    assert result["versions"][0]["bugfix_only"] is True


def test_handles_multiple_version_blocks() -> None:
    text = """
    v0.7.0
    - 800 renders
    - 35 animations

    v0.6.5
    - 200 renders
    """
    result = extract_with_regex(text)
    assert len(result["versions"]) == 2
    versions = {v["version"]: v for v in result["versions"]}
    assert versions["0.7.0"]["renders"] == 800
    assert versions["0.6.5"]["renders"] == 200


def test_empty_input_returns_empty_versions() -> None:
    assert extract_with_regex("") == {"versions": []}
```

- [ ] **Step 2: Run, see red**

Run: `uv run pytest tests/unit/test_llm_fallback_regex.py -v`
Expected: ImportError.

- [ ] **Step 3: Implement fallback_regex.py**

`src/vnmaster/llm/__init__.py`: empty.

`src/vnmaster/llm/fallback_regex.py`:
```python
"""Pure regex-based extractor. Used when LLM is unavailable or budget exhausted.

Lower fidelity than the LLM extractor: no dev summary, brittle to format
variance. Sufficient as a backstop.
"""
from __future__ import annotations

import re
from typing import Any

_VERSION_HEADER = re.compile(r"(?im)^\s*v?(\d+(?:\.\d+)+[a-z]?)\b")
_RENDERS = re.compile(r"(\d+(?:,\d+)?)\s+(?:new\s+)?renders?", re.IGNORECASE)
_ANIMATIONS = re.compile(r"(\d+(?:,\d+)?)\s+(?:new\s+)?animations?", re.IGNORECASE)
_WORDS = re.compile(
    r"(\d+(?:,\d+)?)\s+(?:new\s+)?(?:lines?|words?)\s+(?:of\s+)?dialogue",
    re.IGNORECASE,
)
_SCENES = re.compile(r"(\d+)\s+(?:new\s+)?scenes?", re.IGNORECASE)
_LOCATIONS = re.compile(r"(\d+)\s+(?:new\s+)?locations?", re.IGNORECASE)
_CHARACTERS = re.compile(r"(\d+)\s+(?:new\s+)?characters?", re.IGNORECASE)
_BUGFIX_HINT = re.compile(r"\bbug\s*fix(?:es)?\b|\bfixes\b", re.IGNORECASE)


def extract_with_regex(text: str) -> dict[str, Any]:
    if not text.strip():
        return {"versions": []}

    sections = _split_by_version(text)
    out: list[dict[str, Any]] = []
    for version, body in sections:
        renders = _first_int(_RENDERS, body)
        animations = _first_int(_ANIMATIONS, body)
        words = _first_int(_WORDS, body)
        scenes = _first_int(_SCENES, body)
        locations = _first_int(_LOCATIONS, body)
        characters = _first_int(_CHARACTERS, body)
        content_signals = any(
            x is not None for x in (renders, animations, words, scenes, locations, characters)
        )
        bugfix_only = (not content_signals) and bool(_BUGFIX_HINT.search(body))
        out.append({
            "version": version,
            "released_at": None,
            "renders": renders,
            "animations": animations,
            "words": words,
            "scenes": scenes,
            "new_locations": locations,
            "new_characters": characters,
            "bugfix_only": bugfix_only,
            "summary_one_line": "",
        })
    return {"versions": out}


def _split_by_version(text: str) -> list[tuple[str, str]]:
    matches = list(_VERSION_HEADER.finditer(text))
    if not matches:
        return []
    sections = []
    for i, m in enumerate(matches):
        version = m.group(1)
        body_start = m.end()
        body_end = matches[i + 1].start() if i + 1 < len(matches) else len(text)
        sections.append((version, text[body_start:body_end]))
    return sections


def _first_int(pattern: re.Pattern[str], body: str) -> int | None:
    m = pattern.search(body)
    if not m:
        return None
    return int(m.group(1).replace(",", ""))
```

- [ ] **Step 4: Run tests, see green**

Run: `uv run pytest tests/unit/test_llm_fallback_regex.py -v`
Expected: 4 passed.

- [ ] **Step 5: Commit**

```bash
git add src/vnmaster/llm/__init__.py src/vnmaster/llm/fallback_regex.py tests/unit/test_llm_fallback_regex.py
git commit -m "feat: regex-based changelog extractor as LLM fallback"
```

---

### Task 8.2: Anthropic SDK extractor with prompt caching

**Files:**
- Create: `src/vnmaster/llm/prompts.py`
- Create: `src/vnmaster/llm/changelog.py`
- Create: `tests/unit/test_llm_changelog.py` (unit, all mocked)
- Create: `tests/integration/test_llm_changelog_live.py` (gated)
- Create: `tests/fixtures/changelogs/v07_eternum.txt` (one sample)

- [ ] **Step 1: Build the system prompt**

`src/vnmaster/llm/prompts.py`:
```python
"""System prompt + JSON tool schema for the Anthropic extractor.

The system prompt is constant per process and stable across runs — perfect
candidate for prompt caching with cache_control.
"""
from __future__ import annotations

SYSTEM_PROMPT = """You are a precise data-extraction assistant for adult-VN
changelogs. Given a raw changelog from F95Zone (which may cover one or more
versions), call the `record_changelog` tool exactly once with a structured
list of versions.

Rules:
- For each version block, emit one object.
- Integers must come from the text. If a metric is absent, use null.
- `bugfix_only` is true only when the version contains NO content additions
  (no renders, animations, words, scenes, locations, characters) AND the
  text mentions bugfixes/maintenance.
- `summary_one_line` is your concise (≤80 chars) summary of the *content*
  changes (not bugs). If no content changes, return "".
- Preserve the developer's version label as-is (e.g., "0.7.0", "0.7a", "1.0").
- If `released_at` isn't clearly stated, return null.
- Do not invent numbers. When in doubt, prefer null over guessing.
"""

TOOL_SCHEMA = {
    "name": "record_changelog",
    "description": "Record the structured per-version delta data.",
    "input_schema": {
        "type": "object",
        "properties": {
            "versions": {
                "type": "array",
                "items": {
                    "type": "object",
                    "properties": {
                        "version": {"type": "string"},
                        "released_at": {"type": ["string", "null"]},
                        "renders": {"type": ["integer", "null"]},
                        "animations": {"type": ["integer", "null"]},
                        "words": {"type": ["integer", "null"]},
                        "scenes": {"type": ["integer", "null"]},
                        "new_locations": {"type": ["integer", "null"]},
                        "new_characters": {"type": ["integer", "null"]},
                        "bugfix_only": {"type": "boolean"},
                        "summary_one_line": {"type": "string"},
                    },
                    "required": ["version", "bugfix_only", "summary_one_line"],
                },
            }
        },
        "required": ["versions"],
    },
}
```

- [ ] **Step 2: Write the failing test (mocked SDK)**

`tests/fixtures/changelogs/v07_eternum.txt`:
```
v0.7.0 — April 2026
- 800 new renders
- 35 animations
- 12,000 lines of dialogue
- New chapter at the university
- New love interest: Maya
```

`tests/unit/test_llm_changelog.py`:
```python
from pathlib import Path
from unittest.mock import MagicMock

import pytest

from vnmaster.llm.changelog import (
    BudgetExceeded,
    ChangelogExtractor,
    ExtractionResult,
)

FIXTURE = Path(__file__).parent.parent / "fixtures" / "changelogs" / "v07_eternum.txt"


def _fake_response(payload: dict) -> MagicMock:
    response = MagicMock()
    block = MagicMock()
    block.type = "tool_use"
    block.input = payload
    response.content = [block]
    response.usage.input_tokens = 800
    response.usage.cache_read_input_tokens = 600
    response.usage.cache_creation_input_tokens = 200
    response.usage.output_tokens = 120
    return response


def test_extract_returns_versions_from_tool_use() -> None:
    client = MagicMock()
    client.messages.create.return_value = _fake_response(
        {"versions": [{
            "version": "0.7.0", "released_at": "2026-04-01",
            "renders": 800, "animations": 35, "words": 12000,
            "scenes": None, "new_locations": 1, "new_characters": 1,
            "bugfix_only": False, "summary_one_line": "Maya story + university",
        }]}
    )
    extractor = ChangelogExtractor(
        client=client, model="claude-haiku-4-5",
        budget_tracker=MagicMock(remaining_usd=lambda: 10.0, record=lambda *a, **k: None),
    )
    result = extractor.extract(FIXTURE.read_text(), title="Eternum")
    assert isinstance(result, ExtractionResult)
    assert result.method == "llm"
    assert len(result.versions) == 1
    assert result.versions[0]["renders"] == 800
    assert result.cost_usd > 0


def test_budget_exhausted_raises() -> None:
    client = MagicMock()
    budget = MagicMock()
    budget.remaining_usd.return_value = 0.0
    extractor = ChangelogExtractor(
        client=client, model="claude-haiku-4-5", budget_tracker=budget
    )
    with pytest.raises(BudgetExceeded):
        extractor.extract("anything", title="X")
    client.messages.create.assert_not_called()


def test_extractor_passes_cache_control_on_system_prompt() -> None:
    client = MagicMock()
    client.messages.create.return_value = _fake_response({"versions": []})
    extractor = ChangelogExtractor(
        client=client, model="claude-haiku-4-5",
        budget_tracker=MagicMock(remaining_usd=lambda: 10.0, record=lambda *a, **k: None),
    )
    extractor.extract("v0.1\n- 1 render", title="X")
    kwargs = client.messages.create.call_args.kwargs
    system_param = kwargs["system"]
    assert isinstance(system_param, list)
    assert system_param[0].get("cache_control") == {"type": "ephemeral"}
```

- [ ] **Step 3: Run, see red**

Run: `uv run pytest tests/unit/test_llm_changelog.py -v`
Expected: ImportError.

- [ ] **Step 4: Implement changelog.py**

```python
"""Anthropic-based changelog extractor.

Uses prompt caching on the system block so per-call cost is dominated by the
short user message (the changelog body).
"""
from __future__ import annotations

from dataclasses import dataclass
from typing import Any, Protocol

from anthropic import Anthropic

from vnmaster.llm.prompts import SYSTEM_PROMPT, TOOL_SCHEMA


# Pricing — claude-haiku-4-5 per 1M tokens (USD). Update if Anthropic re-prices.
_INPUT_PER_M = 1.00
_OUTPUT_PER_M = 5.00
_CACHE_WRITE_PER_M = 1.25
_CACHE_READ_PER_M = 0.10


class BudgetExceeded(Exception):
    pass


class BudgetTracker(Protocol):
    def remaining_usd(self) -> float: ...
    def record(self, cost_usd: float) -> None: ...


@dataclass(frozen=True)
class ExtractionResult:
    method: str  # 'llm' | 'regex_fallback'
    versions: list[dict[str, Any]]
    cost_usd: float


class ChangelogExtractor:
    def __init__(
        self, client: Anthropic, model: str, budget_tracker: BudgetTracker
    ) -> None:
        self._client = client
        self._model = model
        self._budget = budget_tracker

    def extract(self, raw_changelog: str, *, title: str) -> ExtractionResult:
        if self._budget.remaining_usd() <= 0:
            raise BudgetExceeded("monthly LLM budget exhausted")

        response = self._client.messages.create(
            model=self._model,
            max_tokens=1024,
            system=[
                {
                    "type": "text",
                    "text": SYSTEM_PROMPT,
                    "cache_control": {"type": "ephemeral"},
                }
            ],
            tools=[TOOL_SCHEMA],
            tool_choice={"type": "tool", "name": "record_changelog"},
            messages=[
                {
                    "role": "user",
                    "content": f"Title: {title}\n\nChangelog:\n{raw_changelog}",
                }
            ],
        )

        versions = _extract_tool_output(response)
        cost = _compute_cost(response)
        self._budget.record(cost)

        return ExtractionResult(method="llm", versions=versions, cost_usd=cost)


def _extract_tool_output(response: Any) -> list[dict[str, Any]]:
    for block in response.content:
        if getattr(block, "type", None) == "tool_use":
            payload = block.input
            return list(payload.get("versions", []))
    return []


def _compute_cost(response: Any) -> float:
    u = response.usage
    return (
        getattr(u, "input_tokens", 0) / 1_000_000 * _INPUT_PER_M
        + getattr(u, "cache_creation_input_tokens", 0) / 1_000_000 * _CACHE_WRITE_PER_M
        + getattr(u, "cache_read_input_tokens", 0) / 1_000_000 * _CACHE_READ_PER_M
        + getattr(u, "output_tokens", 0) / 1_000_000 * _OUTPUT_PER_M
    )
```

- [ ] **Step 5: Run tests, see green**

Run: `uv run pytest tests/unit/test_llm_changelog.py -v`
Expected: 3 passed.

- [ ] **Step 6: Live-API integration test (gated)**

`tests/integration/test_llm_changelog_live.py`:
```python
import os
from pathlib import Path

import pytest
from anthropic import Anthropic

from vnmaster.llm.budget import InMemoryBudget
from vnmaster.llm.changelog import ChangelogExtractor

FIXTURE = Path(__file__).parent.parent / "fixtures" / "changelogs" / "v07_eternum.txt"


@pytest.mark.live_llm
@pytest.mark.skipif(
    os.environ.get("VNMASTER_LIVE_LLM") != "1",
    reason="set VNMASTER_LIVE_LLM=1 to hit the real Anthropic API",
)
def test_live_extraction_matches_expectations() -> None:
    client = Anthropic()
    extractor = ChangelogExtractor(
        client=client,
        model="claude-haiku-4-5",
        budget_tracker=InMemoryBudget(cap_usd=1.0),
    )
    result = extractor.extract(FIXTURE.read_text(), title="Eternum")
    assert result.method == "llm"
    v = result.versions[0]
    assert v["version"] == "0.7.0"
    assert v["renders"] == 800
    assert v["animations"] == 35
```

- [ ] **Step 7: Commit**

```bash
git add src/vnmaster/llm/prompts.py src/vnmaster/llm/changelog.py tests/unit/test_llm_changelog.py tests/integration/test_llm_changelog_live.py tests/fixtures/changelogs/
git commit -m "feat: Anthropic-backed changelog extractor with prompt caching + cost tracking"
```

---

### Task 8.3: Budget tracker

**Files:**
- Create: `src/vnmaster/llm/budget.py`
- Create: `tests/unit/test_llm_budget.py`

- [ ] **Step 1: Write the failing test**

`tests/unit/test_llm_budget.py`:
```python
from vnmaster.llm.budget import InMemoryBudget


def test_initial_remaining_equals_cap() -> None:
    b = InMemoryBudget(cap_usd=5.0)
    assert b.remaining_usd() == 5.0


def test_record_subtracts_from_remaining() -> None:
    b = InMemoryBudget(cap_usd=5.0)
    b.record(1.25)
    assert b.remaining_usd() == 3.75


def test_record_can_go_negative_but_remaining_clamps_to_zero() -> None:
    b = InMemoryBudget(cap_usd=1.0)
    b.record(1.5)
    assert b.remaining_usd() == 0.0


def test_reset_restores_full_cap() -> None:
    b = InMemoryBudget(cap_usd=5.0)
    b.record(2.0)
    b.reset()
    assert b.remaining_usd() == 5.0
```

- [ ] **Step 2: Run, see red**

Run: `uv run pytest tests/unit/test_llm_budget.py -v`
Expected: ImportError.

- [ ] **Step 3: Implement budget.py**

```python
"""Tracks LLM spend per process.

For v1.0 we keep it in memory; a persistent variant reading/writing to
vnmaster.db could be added in v1.1.
"""
from __future__ import annotations


class InMemoryBudget:
    def __init__(self, cap_usd: float) -> None:
        self._cap = cap_usd
        self._spent = 0.0

    def remaining_usd(self) -> float:
        return max(0.0, self._cap - self._spent)

    def record(self, cost_usd: float) -> None:
        self._spent += cost_usd

    def reset(self) -> None:
        self._spent = 0.0

    @property
    def spent_usd(self) -> float:
        return self._spent
```

- [ ] **Step 4: Run tests, see green**

Run: `uv run pytest tests/unit/test_llm_budget.py -v`
Expected: 4 passed.

- [ ] **Step 5: Commit**

```bash
git add src/vnmaster/llm/budget.py tests/unit/test_llm_budget.py
git commit -m "feat: in-memory LLM budget tracker"
```

---

### Task 8.4: Cache wrapper — re-extract only on content hash change

**Files:**
- Create: `src/vnmaster/llm/cache.py`
- Create: `tests/unit/test_llm_cache.py`

- [ ] **Step 1: Write the failing test**

`tests/unit/test_llm_cache.py`:
```python
import hashlib
import json
from pathlib import Path

from sqlalchemy import select

from vnmaster.db.engine import create_engine_for, session_scope
from vnmaster.db.models import Base, ChangelogExtraction
from vnmaster.llm.cache import CachedExtractor
from vnmaster.llm.changelog import ExtractionResult


class _FakeExtractor:
    def __init__(self) -> None:
        self.calls = 0

    def extract(self, raw_changelog: str, *, title: str) -> ExtractionResult:
        self.calls += 1
        return ExtractionResult(
            method="llm",
            versions=[{"version": "0.1.0", "bugfix_only": False}],
            cost_usd=0.01,
        )


def _setup_db(tmp_path: Path):
    engine = create_engine_for(tmp_path / "v.db")
    Base.metadata.create_all(engine)
    return engine


def test_first_call_invokes_inner_extractor(tmp_path: Path) -> None:
    engine = _setup_db(tmp_path)
    inner = _FakeExtractor()
    cached = CachedExtractor(inner=inner, engine=engine, clock=lambda: 100)
    result = cached.extract_for(thread_id=1, raw="changelog body", title="X")
    assert inner.calls == 1
    assert result.method == "llm"


def test_second_call_same_content_uses_cache(tmp_path: Path) -> None:
    engine = _setup_db(tmp_path)
    inner = _FakeExtractor()
    cached = CachedExtractor(inner=inner, engine=engine, clock=lambda: 100)
    cached.extract_for(thread_id=1, raw="body", title="X")
    cached.extract_for(thread_id=1, raw="body", title="X")
    assert inner.calls == 1


def test_content_change_invalidates_cache(tmp_path: Path) -> None:
    engine = _setup_db(tmp_path)
    inner = _FakeExtractor()
    cached = CachedExtractor(inner=inner, engine=engine, clock=lambda: 100)
    cached.extract_for(thread_id=1, raw="body A", title="X")
    cached.extract_for(thread_id=1, raw="body B", title="X")
    assert inner.calls == 2


def test_cache_persists_to_db(tmp_path: Path) -> None:
    engine = _setup_db(tmp_path)
    inner = _FakeExtractor()
    cached = CachedExtractor(inner=inner, engine=engine, clock=lambda: 100)
    cached.extract_for(thread_id=1, raw="payload", title="X")
    with session_scope(engine) as s:
        row = s.execute(select(ChangelogExtraction)).scalar_one()
        assert row.f95_thread_id == 1
        expected_hash = hashlib.sha256(b"payload").hexdigest()
        assert row.content_hash == expected_hash
        assert json.loads(row.versions_json) == [
            {"version": "0.1.0", "bugfix_only": False}
        ]
```

- [ ] **Step 2: Run, see red**

Run: `uv run pytest tests/unit/test_llm_cache.py -v`
Expected: ImportError.

- [ ] **Step 3: Implement cache.py**

```python
"""SQLite-backed cache: extract-once-per-content-hash."""
from __future__ import annotations

import hashlib
import json
from typing import Callable, Protocol

from sqlalchemy import Engine, select

from vnmaster.db.engine import session_scope
from vnmaster.db.models import ChangelogExtraction
from vnmaster.llm.changelog import ExtractionResult


class InnerExtractor(Protocol):
    def extract(self, raw_changelog: str, *, title: str) -> ExtractionResult: ...


class CachedExtractor:
    def __init__(
        self,
        inner: InnerExtractor,
        engine: Engine,
        clock: Callable[[], int],
    ) -> None:
        self._inner = inner
        self._engine = engine
        self._clock = clock

    def extract_for(
        self, thread_id: int, raw: str, title: str
    ) -> ExtractionResult:
        content_hash = hashlib.sha256(raw.encode("utf-8")).hexdigest()
        with session_scope(self._engine) as s:
            existing = s.execute(
                select(ChangelogExtraction).where(
                    ChangelogExtraction.f95_thread_id == thread_id,
                    ChangelogExtraction.content_hash == content_hash,
                )
            ).scalar_one_or_none()
            if existing is not None:
                return ExtractionResult(
                    method=existing.extraction_method,
                    versions=json.loads(existing.versions_json),
                    cost_usd=0.0,
                )

        result = self._inner.extract(raw, title=title)
        with session_scope(self._engine) as s:
            s.add(
                ChangelogExtraction(
                    f95_thread_id=thread_id,
                    content_hash=content_hash,
                    extraction_method=result.method,
                    versions_json=json.dumps(result.versions),
                    extracted_at=self._clock(),
                )
            )
        return result
```

- [ ] **Step 4: Run tests, see green**

Run: `uv run pytest tests/unit/test_llm_cache.py -v`
Expected: 4 passed.

- [ ] **Step 5: Commit**

```bash
git add src/vnmaster/llm/cache.py tests/unit/test_llm_cache.py
git commit -m "feat: hash-keyed extraction cache backed by vnmaster.db"
```

---

## Phase 9 — Magnitude score

### Task 9.1: Score, star band, and version-range aggregation

**Files:**
- Create: `src/vnmaster/magnitude.py`
- Create: `tests/unit/test_magnitude.py`

- [ ] **Step 1: Write the failing test**

`tests/unit/test_magnitude.py`:
```python
import pytest

from vnmaster.config import MagnitudeScoreConfig
from vnmaster.magnitude import score_versions, star_band, sum_since


WEIGHTS = MagnitudeScoreConfig()


def _v(version: str, **kw) -> dict:
    base = {
        "version": version, "renders": None, "animations": None, "words": None,
        "scenes": None, "new_locations": None, "new_characters": None,
        "bugfix_only": False,
    }
    base.update(kw)
    return base


def test_score_renders_only() -> None:
    s = score_versions([_v("0.7.0", renders=100)], WEIGHTS)
    assert s == 100.0  # renders weight is 1.0 by default


def test_score_animations_weighted_higher() -> None:
    s = score_versions([_v("0.7.0", animations=10)], WEIGHTS)
    assert s == 50.0  # animations weight = 5.0


def test_score_words_divided_by_1000() -> None:
    s = score_versions([_v("0.7.0", words=5000)], WEIGHTS)
    assert s == 5.0


def test_bugfix_only_penalty() -> None:
    s = score_versions([_v("0.7.0", bugfix_only=True)], WEIGHTS)
    assert s == -100.0


def test_summed_across_versions() -> None:
    s = score_versions(
        [_v("0.7.0", renders=100), _v("0.6.0", renders=200)], WEIGHTS
    )
    assert s == 300.0


@pytest.mark.parametrize(
    "score, band",
    [(5, "★"), (30, "★★"), (100, "★★★"), (400, "★★★★"), (1000, "★★★★★")],
)
def test_star_band_thresholds(score: float, band: str) -> None:
    assert star_band(score) == band


def test_sum_since_uses_version_compare() -> None:
    versions = [
        _v("0.7.0", renders=100),
        _v("0.6.5", renders=50),
        _v("0.6.0", renders=200),
        _v("0.5.2", renders=1000),
    ]
    s = sum_since(versions, user_version="0.6.0", weights=WEIGHTS)
    assert s == 150.0


def test_sum_since_none_user_version_means_all() -> None:
    versions = [_v("0.7.0", renders=100), _v("0.6.0", renders=50)]
    s = sum_since(versions, user_version=None, weights=WEIGHTS)
    assert s == 150.0
```

- [ ] **Step 2: Run, see red**

Run: `uv run pytest tests/unit/test_magnitude.py -v`
Expected: ImportError.

- [ ] **Step 3: Implement magnitude.py**

```python
"""Magnitude score computation.

`score_versions` aggregates a list of version dicts (from the LLM/regex
extractor) into a single float. `sum_since` filters to versions strictly
newer than `user_version` before summing. `star_band` maps a score to a
1-5 star label for the Discord embed.
"""
from __future__ import annotations

from typing import Any

from vnmaster.config import MagnitudeScoreConfig


def score_versions(
    versions: list[dict[str, Any]], weights: MagnitudeScoreConfig
) -> float:
    total = 0.0
    for v in versions:
        if v.get("bugfix_only"):
            total += weights.bugfix_only_penalty
            continue
        total += (v.get("renders") or 0) * weights.renders
        total += (v.get("animations") or 0) * weights.animations
        total += ((v.get("words") or 0) / 1000.0) * weights.words_per_1k
        total += (v.get("scenes") or 0) * weights.scenes
        total += (v.get("new_locations") or 0) * weights.new_locations
        total += (v.get("new_characters") or 0) * weights.new_characters
    return total


def sum_since(
    versions: list[dict[str, Any]],
    user_version: str | None,
    weights: MagnitudeScoreConfig,
) -> float:
    if user_version is None:
        return score_versions(versions, weights)
    parsed_user = parse_version(user_version)
    newer = [v for v in versions if parse_version(v["version"]) > parsed_user]
    return score_versions(newer, weights)


def star_band(score: float) -> str:
    if score < 10:
        return "★"
    if score < 50:
        return "★★"
    if score < 200:
        return "★★★"
    if score < 800:
        return "★★★★"
    return "★★★★★"


def parse_version(label: str) -> tuple[int, ...]:
    parts: list[int] = []
    for chunk in label.split("."):
        digits = "".join(c for c in chunk if c.isdigit())
        if digits:
            parts.append(int(digits))
        else:
            parts.append(0)
    return tuple(parts)
```

- [ ] **Step 4: Run tests, see green**

Run: `uv run pytest tests/unit/test_magnitude.py -v`
Expected: 8 passed.

- [ ] **Step 5: Commit**

```bash
git add src/vnmaster/magnitude.py tests/unit/test_magnitude.py
git commit -m "feat: magnitude score, sum-since-version, star band"
```

---

## Phase 10 — Discovery feed

### Task 10.1: F95Indexer API client

**Files:**
- Create: `src/vnmaster/discovery/__init__.py`
- Create: `src/vnmaster/discovery/client.py`
- Create: `tests/unit/test_discovery_client.py`

- [ ] **Step 1: Write the failing test**

`tests/unit/test_discovery_client.py`:
```python
import json

import httpx
import pytest

from vnmaster.discovery.client import DiscoveryClient, DiscoveryThread


def test_fetch_latest_parses_response() -> None:
    def handler(request: httpx.Request) -> httpx.Response:
        assert request.url.path == "/latest"
        return httpx.Response(
            200,
            json={
                "threads": [
                    {
                        "id": 12345,
                        "title": "Acolyte",
                        "developer": "EmberStudios",
                        "posted_at": 1700_000_000,
                        "likes": 432,
                        "tags": ["corruption", "slow-burn"],
                        "short_description": "You inherit an estate...",
                        "image_url": "img.png",
                        "version": "0.2",
                    }
                ]
            },
        )

    transport = httpx.MockTransport(handler)
    client = DiscoveryClient(transport=transport)
    threads = client.fetch_latest(days=7)
    assert len(threads) == 1
    t = threads[0]
    assert isinstance(t, DiscoveryThread)
    assert t.id == 12345
    assert t.title == "Acolyte"
    assert t.tags == ["corruption", "slow-burn"]


def test_fetch_latest_passes_days_param() -> None:
    captured: dict = {}

    def handler(request: httpx.Request) -> httpx.Response:
        captured["params"] = dict(request.url.params)
        return httpx.Response(200, json={"threads": []})

    transport = httpx.MockTransport(handler)
    client = DiscoveryClient(transport=transport)
    client.fetch_latest(days=14)
    assert captured["params"]["days"] == "14"


def test_http_error_raises() -> None:
    def handler(request: httpx.Request) -> httpx.Response:
        return httpx.Response(503)

    transport = httpx.MockTransport(handler)
    client = DiscoveryClient(transport=transport)
    with pytest.raises(httpx.HTTPStatusError):
        client.fetch_latest(days=7)
```

- [ ] **Step 2: Run, see red**

Run: `uv run pytest tests/unit/test_discovery_client.py -v`
Expected: ImportError.

- [ ] **Step 3: Implement client.py**

`src/vnmaster/discovery/__init__.py`: empty.

`src/vnmaster/discovery/client.py`:
```python
"""HTTP client for api.f95checker.dev/latest."""
from __future__ import annotations

from dataclasses import dataclass

import httpx


@dataclass(frozen=True)
class DiscoveryThread:
    id: int
    title: str
    developer: str | None
    posted_at: int
    likes: int | None
    tags: list[str]
    short_description: str | None
    image_url: str | None
    version: str | None


class DiscoveryClient:
    BASE_URL = "https://api.f95checker.dev"

    def __init__(self, *, transport: httpx.BaseTransport | None = None) -> None:
        self._client = httpx.Client(
            base_url=self.BASE_URL, transport=transport, timeout=15.0
        )

    def fetch_latest(self, days: int) -> list[DiscoveryThread]:
        response = self._client.get("/latest", params={"days": str(days)})
        response.raise_for_status()
        body = response.json()
        return [
            DiscoveryThread(
                id=int(t["id"]),
                title=t["title"],
                developer=t.get("developer"),
                posted_at=int(t["posted_at"]),
                likes=t.get("likes"),
                tags=list(t.get("tags", [])),
                short_description=t.get("short_description"),
                image_url=t.get("image_url"),
                version=t.get("version"),
            )
            for t in body.get("threads", [])
        ]
```

- [ ] **Step 4: Run tests, see green**

Run: `uv run pytest tests/unit/test_discovery_client.py -v`
Expected: 3 passed.

- [ ] **Step 5: Commit**

```bash
git add src/vnmaster/discovery/__init__.py src/vnmaster/discovery/client.py tests/unit/test_discovery_client.py
git commit -m "feat: api.f95checker.dev /latest client"
```

---

### Task 10.2: Filter pipeline + ranking

**Files:**
- Create: `src/vnmaster/discovery/filter.py`
- Create: `tests/unit/test_discovery_filter.py`

- [ ] **Step 1: Write the failing test**

`tests/unit/test_discovery_filter.py`:
```python
import math

from vnmaster.discovery.client import DiscoveryThread
from vnmaster.discovery.filter import (
    DiscoveryState,
    apply_filter_pipeline,
    rank,
)


def _t(id: int, tags: list[str], posted: int = 1700_000_000, likes: int = 100) -> DiscoveryThread:
    return DiscoveryThread(
        id=id, title=f"Game {id}", developer="Dev", posted_at=posted,
        likes=likes, tags=tags, short_description="...", image_url=None, version="0.1",
    )


def test_drops_threads_already_in_library() -> None:
    threads = [_t(1, ["corruption"]), _t(2, ["corruption"])]
    out = apply_filter_pipeline(
        threads,
        library_thread_ids={1},
        states={},
        include_tags=["corruption"],
        exclude_tags=[],
        now_epoch=1700_000_100,
    )
    ids = {t.id for t in out}
    assert ids == {2}


def test_drops_hidden_threads() -> None:
    threads = [_t(1, ["corruption"]), _t(2, ["corruption"])]
    out = apply_filter_pipeline(
        threads, library_thread_ids=set(),
        states={1: DiscoveryState(hidden=True, skip_until=None)},
        include_tags=["corruption"], exclude_tags=[], now_epoch=1700_000_100,
    )
    assert {t.id for t in out} == {2}


def test_drops_skip_until_active() -> None:
    threads = [_t(1, ["corruption"])]
    out = apply_filter_pipeline(
        threads, library_thread_ids=set(),
        states={1: DiscoveryState(hidden=False, skip_until=1700_001_000)},
        include_tags=["corruption"], exclude_tags=[], now_epoch=1700_000_100,
    )
    assert out == []


def test_skip_until_expired_lets_through() -> None:
    threads = [_t(1, ["corruption"])]
    out = apply_filter_pipeline(
        threads, library_thread_ids=set(),
        states={1: DiscoveryState(hidden=False, skip_until=1700_000_000)},
        include_tags=["corruption"], exclude_tags=[], now_epoch=1700_000_100,
    )
    assert {t.id for t in out} == {1}


def test_any_include_tag_match_passes() -> None:
    threads = [_t(1, ["slow-burn"]), _t(2, ["other"])]
    out = apply_filter_pipeline(
        threads, library_thread_ids=set(), states={},
        include_tags=["corruption", "slow-burn"], exclude_tags=[],
        now_epoch=1700_000_100,
    )
    assert {t.id for t in out} == {1}


def test_any_exclude_tag_blocks() -> None:
    threads = [_t(1, ["corruption", "ntr"]), _t(2, ["corruption"])]
    out = apply_filter_pipeline(
        threads, library_thread_ids=set(), states={},
        include_tags=["corruption"], exclude_tags=["ntr"], now_epoch=1700_000_100,
    )
    assert {t.id for t in out} == {2}


def test_no_include_tags_drops_everything() -> None:
    threads = [_t(1, ["corruption"])]
    out = apply_filter_pipeline(
        threads, library_thread_ids=set(), states={},
        include_tags=[], exclude_tags=[], now_epoch=1700_000_100,
    )
    assert out == []


def test_ranking_recency_dominates_at_equal_likes() -> None:
    t_new = _t(1, ["corruption"], posted=1700_000_000, likes=100)
    t_old = _t(2, ["corruption"], posted=1700_000_000 - 14 * 86400, likes=100)
    ranked = rank([t_old, t_new], now_epoch=1700_000_000, include_tags=["corruption"])
    assert ranked[0].id == 1


def test_ranking_likes_increase_score() -> None:
    a = _t(1, ["corruption"], posted=1700_000_000, likes=10)
    b = _t(2, ["corruption"], posted=1700_000_000, likes=10_000)
    ranked = rank([a, b], now_epoch=1700_000_000, include_tags=["corruption"])
    assert ranked[0].id == 2


def test_recency_score_formula(monkeypatch) -> None:
    from vnmaster.discovery import filter as filter_mod

    score = filter_mod._recency_score(age_days=7)
    assert math.isclose(score, math.exp(-1.0), rel_tol=1e-6)
```

- [ ] **Step 2: Run, see red**

Run: `uv run pytest tests/unit/test_discovery_filter.py -v`
Expected: ImportError.

- [ ] **Step 3: Implement filter.py**

```python
"""Pure-functional discovery filter + ranker. No I/O."""
from __future__ import annotations

import math
from dataclasses import dataclass

from vnmaster.discovery.client import DiscoveryThread


@dataclass(frozen=True)
class DiscoveryState:
    hidden: bool
    skip_until: int | None  # epoch seconds


def apply_filter_pipeline(
    threads: list[DiscoveryThread],
    *,
    library_thread_ids: set[int],
    states: dict[int, DiscoveryState],
    include_tags: list[str],
    exclude_tags: list[str],
    now_epoch: int,
) -> list[DiscoveryThread]:
    include_set = set(include_tags)
    exclude_set = set(exclude_tags)
    if not include_set:
        return []

    out: list[DiscoveryThread] = []
    for t in threads:
        if t.id in library_thread_ids:
            continue
        state = states.get(t.id)
        if state and state.hidden:
            continue
        if state and state.skip_until and state.skip_until > now_epoch:
            continue
        tags = set(t.tags)
        if not (tags & include_set):
            continue
        if tags & exclude_set:
            continue
        out.append(t)
    return out


def rank(
    threads: list[DiscoveryThread],
    *,
    now_epoch: int,
    include_tags: list[str],
) -> list[DiscoveryThread]:
    include_count = max(1, len(include_tags))
    include_set = set(include_tags)

    def score(t: DiscoveryThread) -> float:
        age_days = max(0.0, (now_epoch - t.posted_at) / 86400.0)
        recency = _recency_score(age_days)
        likes = max(t.likes or 0, 1)
        likes_score = min(1.0, math.log10(likes) / 3.0)
        match_count = len(set(t.tags) & include_set) / include_count
        return recency * 0.4 + likes_score * 0.4 + match_count * 0.2

    return sorted(threads, key=score, reverse=True)


def _recency_score(age_days: float) -> float:
    return math.exp(-age_days / 7.0)
```

- [ ] **Step 4: Run tests, see green**

Run: `uv run pytest tests/unit/test_discovery_filter.py -v`
Expected: 10 passed.

- [ ] **Step 5: Commit**

```bash
git add src/vnmaster/discovery/filter.py tests/unit/test_discovery_filter.py
git commit -m "feat: discovery filter pipeline + recency-weighted ranking"
```

---

## Phase 11 — Digest selection and embed builder

### Task 11.1: Digest selection logic

**Files:**
- Create: `src/vnmaster/digest/__init__.py`
- Create: `src/vnmaster/digest/select.py`
- Create: `tests/unit/test_digest_select.py`

- [ ] **Step 1: Write the failing test**

`tests/unit/test_digest_select.py`:
```python
from dataclasses import replace
from pathlib import Path

from sqlalchemy import select

from vnmaster.db.engine import create_engine_for, session_scope
from vnmaster.db.models import (
    Base, DigestEntry, DigestRun, DiscoveryState as DiscoveryStateRow,
    DiscoverySuggestion, LibraryGame,
)
from vnmaster.digest.select import (
    SelectedDiscovery, SelectedUpdate, select_digest_candidates,
)


def _engine(tmp_path: Path):
    e = create_engine_for(tmp_path / "v.db")
    Base.metadata.create_all(e)
    return e


def _seed_library(s, **overrides) -> None:
    row = LibraryGame(
        f95_thread_id=overrides.pop("thread_id", 42),
        game_title=overrides.pop("title", "Eternum"),
        installed_version=overrides.pop("installed_version", "0.5.2"),
        latest_upstream_version=overrides.pop("latest_upstream_version", "0.7.0"),
        upstream_last_updated_at=overrides.pop("upstream_last_updated_at", 200),
        hidden=overrides.pop("hidden", 0),
        acknowledged_version=overrides.pop("acknowledged_version", None),
        last_seen_in_digest_at=overrides.pop("last_seen_in_digest_at", None),
        created_at=100, updated_at=100,
        **overrides,
    )
    s.add(row)


def test_selects_recently_updated_unhidden_games(tmp_path: Path) -> None:
    engine = _engine(tmp_path)
    with session_scope(engine) as s:
        _seed_library(s, thread_id=1, upstream_last_updated_at=300)
        _seed_library(s, thread_id=2, upstream_last_updated_at=50)  # stale
        _seed_library(s, thread_id=3, upstream_last_updated_at=400, hidden=1)
    result = select_digest_candidates(
        engine=engine, previous_digest_run_at=100, now_epoch=500,
        max_repeat_weeks=4,
    )
    ids = {u.f95_thread_id for u in result.updates}
    assert ids == {1}


def test_acknowledged_version_suppresses_updates(tmp_path: Path) -> None:
    engine = _engine(tmp_path)
    with session_scope(engine) as s:
        _seed_library(s, thread_id=1, latest_upstream_version="0.7.0",
                      acknowledged_version="0.7.0", upstream_last_updated_at=300)
        _seed_library(s, thread_id=2, latest_upstream_version="0.7.0",
                      acknowledged_version="0.6.0", upstream_last_updated_at=300)
    result = select_digest_candidates(
        engine=engine, previous_digest_run_at=100, now_epoch=500, max_repeat_weeks=4,
    )
    ids = {u.f95_thread_id for u in result.updates}
    assert ids == {2}


def test_pre_existing_gap_within_repeat_window_is_skipped(tmp_path: Path) -> None:
    engine = _engine(tmp_path)
    one_week = 7 * 86400
    now = 100_000_000
    with session_scope(engine) as s:
        # No fresh upstream update; user is just behind. Was seen 2 weeks ago.
        _seed_library(
            s, thread_id=1,
            installed_version="0.5.0", latest_upstream_version="0.7.0",
            upstream_last_updated_at=now - 4 * one_week,
            last_seen_in_digest_at=now - 2 * one_week,
        )
    result = select_digest_candidates(
        engine=engine, previous_digest_run_at=now - 86400, now_epoch=now,
        max_repeat_weeks=4,
    )
    assert result.updates == []


def test_pre_existing_gap_past_repeat_window_is_surfaced(tmp_path: Path) -> None:
    engine = _engine(tmp_path)
    one_week = 7 * 86400
    now = 100_000_000
    with session_scope(engine) as s:
        _seed_library(
            s, thread_id=1,
            installed_version="0.5.0", latest_upstream_version="0.7.0",
            upstream_last_updated_at=now - 10 * one_week,
            last_seen_in_digest_at=now - 5 * one_week,
        )
    result = select_digest_candidates(
        engine=engine, previous_digest_run_at=now - 86400, now_epoch=now,
        max_repeat_weeks=4,
    )
    assert {u.f95_thread_id for u in result.updates} == {1}


def test_picks_top_n_discovery_by_first_seen(tmp_path: Path) -> None:
    engine = _engine(tmp_path)
    with session_scope(engine) as s:
        for i in range(5):
            s.add(DiscoverySuggestion(
                f95_thread_id=100 + i, title=f"G{i}", posted_at=200 - i,
                first_seen_at=200 - i,
            ))
    result = select_digest_candidates(
        engine=engine, previous_digest_run_at=100, now_epoch=500, max_repeat_weeks=4,
        max_discovery=3,
    )
    assert len(result.discovery) == 3
```

- [ ] **Step 2: Run, see red**

Run: `uv run pytest tests/unit/test_digest_select.py -v`
Expected: ImportError.

- [ ] **Step 3: Implement select.py**

`src/vnmaster/digest/__init__.py`: empty.

`src/vnmaster/digest/select.py`:
```python
"""Selection step of the digest pipeline.

Pure read against vnmaster.db. Returns plain dataclasses for downstream
embed building.
"""
from __future__ import annotations

from dataclasses import dataclass

from sqlalchemy import Engine, select

from vnmaster.db.engine import session_scope
from vnmaster.db.models import DiscoverySuggestion, LibraryGame


@dataclass(frozen=True)
class SelectedUpdate:
    f95_thread_id: int
    game_title: str
    installed_version: str | None
    latest_upstream_version: str | None
    upstream_last_updated_at: int | None
    raw_changelog: str | None
    developer: str | None
    image_url: str | None
    upstream_thread_url: str | None
    last_played_at: int | None
    install_path: str | None
    tags_json: str | None


@dataclass(frozen=True)
class SelectedDiscovery:
    f95_thread_id: int
    title: str
    developer: str | None
    posted_at: int
    likes: int | None
    tags_json: str | None
    short_description: str | None
    image_url: str | None


@dataclass(frozen=True)
class DigestCandidates:
    updates: list[SelectedUpdate]
    discovery: list[SelectedDiscovery]


def select_digest_candidates(
    *,
    engine: Engine,
    previous_digest_run_at: int,
    now_epoch: int,
    max_repeat_weeks: int,
    max_discovery: int = 10,
) -> DigestCandidates:
    repeat_window = max_repeat_weeks * 7 * 86400
    updates: list[SelectedUpdate] = []
    discovery: list[SelectedDiscovery] = []

    with session_scope(engine) as s:
        for g in s.execute(select(LibraryGame)).scalars().all():
            if g.hidden:
                continue
            if (
                g.acknowledged_version is not None
                and g.acknowledged_version == g.latest_upstream_version
            ):
                continue

            fresh_update = (
                g.upstream_last_updated_at is not None
                and g.upstream_last_updated_at > previous_digest_run_at
            )
            user_behind = (
                g.installed_version is not None
                and g.latest_upstream_version is not None
                and g.installed_version != g.latest_upstream_version
                and (
                    g.last_seen_in_digest_at is None
                    or now_epoch - g.last_seen_in_digest_at > repeat_window
                )
            )
            if not (fresh_update or user_behind):
                continue

            updates.append(
                SelectedUpdate(
                    f95_thread_id=g.f95_thread_id,
                    game_title=g.game_title,
                    installed_version=g.installed_version,
                    latest_upstream_version=g.latest_upstream_version,
                    upstream_last_updated_at=g.upstream_last_updated_at,
                    raw_changelog=g.raw_changelog,
                    developer=g.developer,
                    image_url=g.image_url,
                    upstream_thread_url=g.upstream_thread_url,
                    last_played_at=g.last_played_at,
                    install_path=g.install_path,
                    tags_json=g.tags_json,
                )
            )

        for d in (
            s.execute(select(DiscoverySuggestion).order_by(DiscoverySuggestion.first_seen_at.desc()))
            .scalars()
            .all()[:max_discovery]
        ):
            discovery.append(
                SelectedDiscovery(
                    f95_thread_id=d.f95_thread_id,
                    title=d.title,
                    developer=d.developer,
                    posted_at=d.posted_at,
                    likes=d.likes,
                    tags_json=d.tags_json,
                    short_description=d.short_description,
                    image_url=d.image_url,
                )
            )

    return DigestCandidates(updates=updates, discovery=discovery)
```

- [ ] **Step 4: Run tests, see green**

Run: `uv run pytest tests/unit/test_digest_select.py -v`
Expected: 5 passed.

- [ ] **Step 5: Commit**

```bash
git add src/vnmaster/digest/__init__.py src/vnmaster/digest/select.py tests/unit/test_digest_select.py
git commit -m "feat: digest selection over library + discovery tables"
```

---

### Task 11.2: Embed builders

**Files:**
- Create: `src/vnmaster/digest/embeds.py`
- Create: `tests/unit/test_digest_embeds.py`

- [ ] **Step 1: Write the failing test**

`tests/unit/test_digest_embeds.py`:
```python
import json

from vnmaster.digest.embeds import build_discovery_embed, build_update_embed
from vnmaster.digest.select import SelectedDiscovery, SelectedUpdate


def test_update_embed_includes_version_delta_and_magnitude_stars() -> None:
    sel = SelectedUpdate(
        f95_thread_id=42, game_title="Eternum",
        installed_version="0.5.2", latest_upstream_version="0.7.0",
        upstream_last_updated_at=1700_000_000, raw_changelog="", developer="Caribdis",
        image_url="http://example.com/cover.png", upstream_thread_url="http://f95/42",
        last_played_at=1690_000_000, install_path=None, tags_json='["corruption"]',
    )
    embed = build_update_embed(
        sel,
        deltas={"renders": 800, "animations": 35, "words": 12000, "scenes": 3,
                "new_locations": 1, "new_characters": 1},
        magnitude_score=900.0,
        summary_one_line="Maya story + university chapter",
    )
    assert "Eternum" in embed["title"]
    assert "0.7.0" in embed["title"]
    assert "0.5.2" in embed["title"]
    assert embed["color"] == 0xF0B232
    # Magnitude field with stars
    field = next(f for f in embed["fields"] if f["name"] == "Magnitude score")
    assert "★★★★★" in field["value"]
    assert "Maya story" in field["value"]
    # Delta field includes humanized values
    deltas_field = next(f for f in embed["fields"] if f["name"] == "Since you last played")
    assert "+800 renders" in deltas_field["value"]
    assert "+35 animations" in deltas_field["value"]
    assert "+12k words" in deltas_field["value"]


def test_update_embed_uses_never_played_for_null_installed() -> None:
    sel = SelectedUpdate(
        f95_thread_id=42, game_title="X", installed_version=None,
        latest_upstream_version="0.1", upstream_last_updated_at=None,
        raw_changelog="", developer=None, image_url=None,
        upstream_thread_url=None, last_played_at=None, install_path=None,
        tags_json=None,
    )
    embed = build_update_embed(sel, deltas={}, magnitude_score=0.0, summary_one_line="")
    assert "never played" in embed["title"].lower()


def test_discovery_embed_lists_matching_tags_only() -> None:
    sel = SelectedDiscovery(
        f95_thread_id=99, title="Acolyte", developer="Ember",
        posted_at=1700_000_000, likes=432,
        tags_json=json.dumps(["corruption", "slow-burn", "other"]),
        short_description="You inherit an estate...", image_url=None,
    )
    embed = build_discovery_embed(sel, matching_tags=["corruption", "slow-burn"])
    assert "[NEW]" in embed["title"]
    field = next(f for f in embed["fields"] if f["name"] == "Tags matching your filters")
    assert "corruption" in field["value"]
    assert "slow-burn" in field["value"]
    assert "other" not in field["value"]
    assert embed["color"] == 0x23A55A
```

- [ ] **Step 2: Run, see red**

Run: `uv run pytest tests/unit/test_digest_embeds.py -v`
Expected: ImportError.

- [ ] **Step 3: Implement embeds.py**

```python
"""Discord embed JSON builders for update and discovery items."""
from __future__ import annotations

import json
from typing import Any

from vnmaster.digest.select import SelectedDiscovery, SelectedUpdate
from vnmaster.magnitude import star_band

UPDATE_COLOR = 0xF0B232
DISCOVERY_COLOR = 0x23A55A


def build_update_embed(
    sel: SelectedUpdate,
    *,
    deltas: dict[str, int],
    magnitude_score: float,
    summary_one_line: str,
) -> dict[str, Any]:
    yours = sel.installed_version or "never played"
    title = (
        f"{sel.game_title} — v{sel.latest_upstream_version or '?'} "
        f"(yours: v{yours})"
        if sel.installed_version
        else f"{sel.game_title} — v{sel.latest_upstream_version or '?'} (never played)"
    )
    install_state = "currently installed" if sel.install_path else "not installed"
    description_bits = [sel.developer or "?", "Ren'Py", install_state]
    description = " · ".join(b for b in description_bits if b)

    delta_lines = []
    if deltas.get("renders"):
        delta_lines.append(f"+{deltas['renders']} renders")
    if deltas.get("animations"):
        delta_lines.append(f"+{deltas['animations']} animations")
    if deltas.get("words"):
        delta_lines.append(f"+{deltas['words'] // 1000}k words")
    if deltas.get("scenes"):
        delta_lines.append(f"{deltas['scenes']} new scenes")
    if deltas.get("new_locations"):
        delta_lines.append(f"{deltas['new_locations']} new locations")

    stars = star_band(magnitude_score)
    summary_text = f"{stars}\n{summary_one_line}" if summary_one_line else stars

    embed: dict[str, Any] = {
        "title": title,
        "description": description,
        "color": UPDATE_COLOR,
        "fields": [
            {
                "name": "Since you last played",
                "value": "\n".join(delta_lines) or "(no structured deltas extracted)",
                "inline": True,
            },
            {
                "name": "Magnitude score",
                "value": summary_text,
                "inline": True,
            },
        ],
        "footer": {"text": f"thread #{sel.f95_thread_id}"},
    }
    if sel.image_url:
        embed["thumbnail"] = {"url": sel.image_url}
    if sel.upstream_thread_url:
        embed["url"] = sel.upstream_thread_url
    return embed


def build_discovery_embed(
    sel: SelectedDiscovery, *, matching_tags: list[str]
) -> dict[str, Any]:
    likes_text = f"{sel.likes} likes" if sel.likes else "no likes yet"
    description = " · ".join(
        x for x in [sel.developer or "?", "Ren'Py", "ongoing", likes_text] if x
    )

    fields = [
        {
            "name": "Tags matching your filters",
            "value": " · ".join(matching_tags) or "(none)",
            "inline": False,
        }
    ]
    if sel.short_description:
        fields.append({
            "name": "Overview",
            "value": (sel.short_description or "")[:200],
            "inline": False,
        })

    return {
        "title": f"{sel.title} [NEW]",
        "description": description,
        "color": DISCOVERY_COLOR,
        "fields": fields,
        "footer": {"text": f"thread #{sel.f95_thread_id} · ✅ to track"},
        **({"thumbnail": {"url": sel.image_url}} if sel.image_url else {}),
    }
```

- [ ] **Step 4: Run tests, see green**

Run: `uv run pytest tests/unit/test_digest_embeds.py -v`
Expected: 3 passed.

- [ ] **Step 5: Commit**

```bash
git add src/vnmaster/digest/embeds.py tests/unit/test_digest_embeds.py
git commit -m "feat: Discord embed builders for update + discovery items"
```

---

## Phase 12 — Discord webhook poster

### Task 12.1: Webhook poster and reaction adder

**Files:**
- Create: `src/vnmaster/digest/poster.py`
- Create: `tests/unit/test_digest_poster.py`

- [ ] **Step 1: Write the failing test**

`tests/unit/test_digest_poster.py`:
```python
from unittest.mock import AsyncMock, MagicMock

import pytest

from vnmaster.digest.poster import DiscordPoster, PostedDigest


@pytest.mark.asyncio
async def test_post_sends_kickoff_then_embeds() -> None:
    webhook = AsyncMock()
    webhook.send.return_value = MagicMock(id="msg123")
    bot_client = MagicMock()
    bot_client.add_reaction = AsyncMock()
    bot_client.fetch_channel = AsyncMock()

    poster = DiscordPoster(
        webhook=webhook,
        bot_client=bot_client,
        channel_id="channel-1",
    )
    update_embed = {"title": "U", "color": 0}
    discovery_embed = {"title": "D", "color": 0}
    result = await poster.post(
        kickoff_text="Weekly",
        update_embeds=[(42, update_embed)],
        discovery_embeds=[(99, discovery_embed)],
    )
    assert isinstance(result, PostedDigest)
    # Kickoff + one update + one discovery
    assert webhook.send.await_count == 3
    # Reactions: 3 on update, 3 on discovery
    assert bot_client.add_reaction.await_count == 6


@pytest.mark.asyncio
async def test_each_message_carries_one_embed() -> None:
    webhook = AsyncMock()
    webhook.send.return_value = MagicMock(id="m")
    bot_client = MagicMock()
    bot_client.add_reaction = AsyncMock()
    poster = DiscordPoster(webhook=webhook, bot_client=bot_client, channel_id="c")
    await poster.post(
        kickoff_text="W",
        update_embeds=[(1, {}), (2, {}), (3, {})],
        discovery_embeds=[],
    )
    # 1 kickoff + 3 updates = 4 calls. Each update call has 1 embed.
    update_calls = webhook.send.call_args_list[1:]
    for call in update_calls:
        assert len(call.kwargs["embeds"]) == 1


@pytest.mark.asyncio
async def test_post_records_message_ids_per_thread() -> None:
    webhook = AsyncMock()
    webhook.send.side_effect = [
        MagicMock(id="k"),
        MagicMock(id="u-1"),
        MagicMock(id="d-1"),
    ]
    bot_client = MagicMock()
    bot_client.add_reaction = AsyncMock()
    poster = DiscordPoster(webhook=webhook, bot_client=bot_client, channel_id="c")
    result = await poster.post(
        kickoff_text="W",
        update_embeds=[(42, {})],
        discovery_embeds=[(99, {})],
    )
    assert result.entries == [
        ("u-1", 0, "update", 42),
        ("d-1", 0, "discovery", 99),
    ]
```

- [ ] **Step 2: Run, see red**

Run: `uv run pytest tests/unit/test_digest_poster.py -v`
Expected: ImportError.

- [ ] **Step 3: Implement poster.py**

```python
"""Posts a digest to Discord and stamps reactions.

Posting goes through a webhook (no auth, no rate limit headaches), but
reactions require a bot-authenticated client because webhooks can't react.
"""
from __future__ import annotations

from dataclasses import dataclass, field
from typing import Any, Protocol


UPDATE_REACTIONS = ("♻️", "❌", "📥")
DISCOVERY_REACTIONS = ("✅", "❌", "⏭️")


class Webhook(Protocol):
    async def send(
        self, content: str | None = ..., embeds: list[Any] | None = ...
    ) -> Any: ...


class BotClient(Protocol):
    async def add_reaction(
        self, channel_id: str, message_id: str, emoji: str
    ) -> None: ...


@dataclass(frozen=True)
class PostedDigest:
    # tuples of (message_id, embed_index, kind, f95_thread_id)
    entries: list[tuple[str, int, str, int]] = field(default_factory=list)


class DiscordPoster:
    def __init__(
        self, *, webhook: Webhook, bot_client: BotClient, channel_id: str
    ) -> None:
        self._webhook = webhook
        self._bot = bot_client
        self._channel_id = channel_id

    async def post(
        self,
        *,
        kickoff_text: str,
        update_embeds: list[tuple[int, dict[str, Any]]],
        discovery_embeds: list[tuple[int, dict[str, Any]]],
    ) -> PostedDigest:
        await self._webhook.send(content=kickoff_text)

        entries: list[tuple[str, int, str, int]] = []
        for thread_id, embed in update_embeds:
            msg = await self._webhook.send(embeds=[embed])
            entries.append((msg.id, 0, "update", thread_id))
            for emoji in UPDATE_REACTIONS:
                await self._bot.add_reaction(self._channel_id, msg.id, emoji)

        for thread_id, embed in discovery_embeds:
            msg = await self._webhook.send(embeds=[embed])
            entries.append((msg.id, 0, "discovery", thread_id))
            for emoji in DISCOVERY_REACTIONS:
                await self._bot.add_reaction(self._channel_id, msg.id, emoji)

        return PostedDigest(entries=entries)
```

- [ ] **Step 4: Run tests, see green**

Run: `uv run pytest tests/unit/test_digest_poster.py -v`
Expected: 3 passed.

- [ ] **Step 5: Commit**

```bash
git add src/vnmaster/digest/poster.py tests/unit/test_digest_poster.py
git commit -m "feat: webhook poster + bot-client reaction stamper"
```

---

## Phase 13 — Discord reaction bot

### Task 13.1: Pure reaction-application function

**Files:**
- Create: `src/vnmaster/bot/__init__.py`
- Create: `src/vnmaster/bot/apply_reaction.py`
- Create: `tests/unit/test_apply_reaction.py`

- [ ] **Step 1: Write the failing test**

`tests/unit/test_apply_reaction.py`:
```python
from pathlib import Path

from sqlalchemy import select

from vnmaster.bot.apply_reaction import ReactionAction, apply_reaction
from vnmaster.db.engine import create_engine_for, session_scope
from vnmaster.db.models import Base, DiscoveryState, LibraryGame


def _engine(tmp_path: Path):
    e = create_engine_for(tmp_path / "v.db")
    Base.metadata.create_all(e)
    return e


def _seed_library_game(engine, thread_id=42, **overrides) -> None:
    with session_scope(engine) as s:
        defaults = dict(
            f95_thread_id=thread_id, game_title="Eternum",
            latest_upstream_version="0.7.0", created_at=1, updated_at=1,
        )
        defaults.update(overrides)
        s.add(LibraryGame(**defaults))


def test_update_hide_sets_hidden_flag(tmp_path: Path) -> None:
    engine = _engine(tmp_path)
    _seed_library_game(engine)
    apply_reaction(
        engine=engine, thread_id=42, kind="update",
        action=ReactionAction.HIDE, now_epoch=100,
    )
    with session_scope(engine) as s:
        g = s.execute(select(LibraryGame).where(LibraryGame.f95_thread_id == 42)).scalar_one()
        assert g.hidden == 1


def test_update_interested_sets_flag(tmp_path: Path) -> None:
    engine = _engine(tmp_path)
    _seed_library_game(engine)
    apply_reaction(
        engine=engine, thread_id=42, kind="update",
        action=ReactionAction.INTERESTED, now_epoch=100,
    )
    with session_scope(engine) as s:
        g = s.execute(select(LibraryGame).where(LibraryGame.f95_thread_id == 42)).scalar_one()
        assert g.interested == 1


def test_update_acknowledged_records_current_version(tmp_path: Path) -> None:
    engine = _engine(tmp_path)
    _seed_library_game(engine, latest_upstream_version="0.9.1")
    apply_reaction(
        engine=engine, thread_id=42, kind="update",
        action=ReactionAction.ACKNOWLEDGED, now_epoch=100,
    )
    with session_scope(engine) as s:
        g = s.execute(select(LibraryGame).where(LibraryGame.f95_thread_id == 42)).scalar_one()
        assert g.acknowledged_version == "0.9.1"


def test_discovery_hide_inserts_state_row(tmp_path: Path) -> None:
    engine = _engine(tmp_path)
    apply_reaction(
        engine=engine, thread_id=999, kind="discovery",
        action=ReactionAction.HIDE, now_epoch=100,
    )
    with session_scope(engine) as s:
        row = s.execute(select(DiscoveryState)).scalar_one()
        assert row.f95_thread_id == 999
        assert row.hidden == 1


def test_discovery_skip_sets_skip_until_four_weeks(tmp_path: Path) -> None:
    engine = _engine(tmp_path)
    apply_reaction(
        engine=engine, thread_id=999, kind="discovery",
        action=ReactionAction.SKIP, now_epoch=1_000_000_000,
        skip_window_weeks=4,
    )
    with session_scope(engine) as s:
        row = s.execute(select(DiscoveryState)).scalar_one()
        assert row.skip_until == 1_000_000_000 + 4 * 7 * 86400


def test_discovery_interested_sets_interested_flag(tmp_path: Path) -> None:
    engine = _engine(tmp_path)
    apply_reaction(
        engine=engine, thread_id=999, kind="discovery",
        action=ReactionAction.INTERESTED, now_epoch=100,
    )
    with session_scope(engine) as s:
        row = s.execute(select(DiscoveryState)).scalar_one()
        assert row.interested == 1


def test_unknown_kind_raises(tmp_path: Path) -> None:
    engine = _engine(tmp_path)
    import pytest
    with pytest.raises(ValueError):
        apply_reaction(
            engine=engine, thread_id=1, kind="garbage",
            action=ReactionAction.HIDE, now_epoch=100,
        )
```

- [ ] **Step 2: Run, see red**

Run: `uv run pytest tests/unit/test_apply_reaction.py -v`
Expected: ImportError.

- [ ] **Step 3: Implement apply_reaction.py**

`src/vnmaster/bot/__init__.py`: empty.

`src/vnmaster/bot/apply_reaction.py`:
```python
"""Pure state-transition function for reactions.

The bot's event handler resolves (message_id, emoji) → (thread_id, kind, action)
and then calls this function. Separated so the state machine can be unit-tested
without discord.py.
"""
from __future__ import annotations

from enum import Enum

from sqlalchemy import Engine, select

from vnmaster.db.engine import session_scope
from vnmaster.db.models import DiscoveryState, LibraryGame


class ReactionAction(str, Enum):
    INTERESTED = "interested"
    HIDE = "hide"
    ACKNOWLEDGED = "acknowledged"  # update only — "📥 already grabbed"
    SKIP = "skip"  # discovery only — "⏭️ skip 4 weeks"


def apply_reaction(
    *,
    engine: Engine,
    thread_id: int,
    kind: str,
    action: ReactionAction,
    now_epoch: int,
    skip_window_weeks: int = 4,
) -> None:
    if kind == "update":
        _apply_update_reaction(
            engine=engine, thread_id=thread_id, action=action, now_epoch=now_epoch
        )
    elif kind == "discovery":
        _apply_discovery_reaction(
            engine=engine, thread_id=thread_id, action=action, now_epoch=now_epoch,
            skip_window_weeks=skip_window_weeks,
        )
    else:
        raise ValueError(f"unknown reaction kind: {kind!r}")


def _apply_update_reaction(
    *, engine: Engine, thread_id: int, action: ReactionAction, now_epoch: int
) -> None:
    with session_scope(engine) as s:
        g = s.execute(
            select(LibraryGame).where(LibraryGame.f95_thread_id == thread_id)
        ).scalar_one_or_none()
        if g is None:
            return
        if action is ReactionAction.HIDE:
            g.hidden = 1
        elif action is ReactionAction.INTERESTED:
            g.interested = 1
        elif action is ReactionAction.ACKNOWLEDGED:
            g.acknowledged_version = g.latest_upstream_version
        else:
            raise ValueError(f"action {action} invalid for kind='update'")
        g.updated_at = now_epoch


def _apply_discovery_reaction(
    *,
    engine: Engine,
    thread_id: int,
    action: ReactionAction,
    now_epoch: int,
    skip_window_weeks: int,
) -> None:
    with session_scope(engine) as s:
        row = s.execute(
            select(DiscoveryState).where(DiscoveryState.f95_thread_id == thread_id)
        ).scalar_one_or_none()
        if row is None:
            row = DiscoveryState(f95_thread_id=thread_id, updated_at=now_epoch)
            s.add(row)
        if action is ReactionAction.HIDE:
            row.hidden = 1
        elif action is ReactionAction.INTERESTED:
            row.interested = 1
        elif action is ReactionAction.SKIP:
            row.skip_until = now_epoch + skip_window_weeks * 7 * 86400
        else:
            raise ValueError(f"action {action} invalid for kind='discovery'")
        row.updated_at = now_epoch
```

- [ ] **Step 4: Run tests, see green**

Run: `uv run pytest tests/unit/test_apply_reaction.py -v`
Expected: 7 passed.

- [ ] **Step 5: Commit**

```bash
git add src/vnmaster/bot/__init__.py src/vnmaster/bot/apply_reaction.py tests/unit/test_apply_reaction.py
git commit -m "feat: pure reaction-application state machine"
```

---

### Task 13.2: Reaction event router

**Files:**
- Create: `src/vnmaster/bot/reactions.py`
- Create: `tests/unit/test_reactions_router.py`

- [ ] **Step 1: Write the failing test**

`tests/unit/test_reactions_router.py`:
```python
from pathlib import Path

import pytest
from sqlalchemy import select

from vnmaster.bot.apply_reaction import ReactionAction
from vnmaster.bot.reactions import resolve_reaction, route_reaction
from vnmaster.db.engine import create_engine_for, session_scope
from vnmaster.db.models import Base, DigestEntry, DigestRun, LibraryGame


def test_resolve_recognized_update_emojis() -> None:
    assert resolve_reaction("update", "♻️") is ReactionAction.INTERESTED
    assert resolve_reaction("update", "❌") is ReactionAction.HIDE
    assert resolve_reaction("update", "📥") is ReactionAction.ACKNOWLEDGED


def test_resolve_recognized_discovery_emojis() -> None:
    assert resolve_reaction("discovery", "✅") is ReactionAction.INTERESTED
    assert resolve_reaction("discovery", "❌") is ReactionAction.HIDE
    assert resolve_reaction("discovery", "⏭️") is ReactionAction.SKIP


def test_resolve_unknown_emoji_returns_none() -> None:
    assert resolve_reaction("update", "🍆") is None


def _engine(tmp_path: Path):
    e = create_engine_for(tmp_path / "v.db")
    Base.metadata.create_all(e)
    return e


def _seed(engine, message_id: str, kind: str, thread_id: int) -> None:
    with session_scope(engine) as s:
        run = DigestRun(
            run_at=1, updates_count=0, discovery_count=0, llm_calls=0, llm_cost_usd=0.0
        )
        s.add(run)
        s.flush()
        s.add(DigestEntry(
            run_id=run.id, discord_message_id=message_id, embed_index=0,
            kind=kind, f95_thread_id=thread_id,
        ))
        s.add(LibraryGame(
            f95_thread_id=thread_id, game_title="X",
            latest_upstream_version="0.1", created_at=1, updated_at=1,
        ))


def test_route_resolves_message_to_thread_and_applies(tmp_path: Path) -> None:
    engine = _engine(tmp_path)
    _seed(engine, message_id="m-1", kind="update", thread_id=42)
    route_reaction(engine=engine, message_id="m-1", emoji="❌", now_epoch=100)
    with session_scope(engine) as s:
        g = s.execute(select(LibraryGame)).scalar_one()
        assert g.hidden == 1


def test_route_ignores_unknown_message(tmp_path: Path) -> None:
    engine = _engine(tmp_path)
    # No raise expected
    route_reaction(engine=engine, message_id="unknown", emoji="❌", now_epoch=100)


def test_route_ignores_unrecognized_emoji(tmp_path: Path) -> None:
    engine = _engine(tmp_path)
    _seed(engine, message_id="m-1", kind="update", thread_id=42)
    route_reaction(engine=engine, message_id="m-1", emoji="🍆", now_epoch=100)
    with session_scope(engine) as s:
        g = s.execute(select(LibraryGame)).scalar_one()
        assert g.hidden == 0
```

- [ ] **Step 2: Run, see red**

Run: `uv run pytest tests/unit/test_reactions_router.py -v`
Expected: ImportError.

- [ ] **Step 3: Implement reactions.py**

```python
"""Reaction emoji → action resolver, and message_id → thread lookup router."""
from __future__ import annotations

from sqlalchemy import Engine, select

from vnmaster.bot.apply_reaction import ReactionAction, apply_reaction
from vnmaster.db.engine import session_scope
from vnmaster.db.models import DigestEntry
from vnmaster.logging_setup import get_logger

log = get_logger(__name__)

_UPDATE_MAP = {
    "♻️": ReactionAction.INTERESTED,
    "❌": ReactionAction.HIDE,
    "📥": ReactionAction.ACKNOWLEDGED,
}
_DISCOVERY_MAP = {
    "✅": ReactionAction.INTERESTED,
    "❌": ReactionAction.HIDE,
    "⏭️": ReactionAction.SKIP,
}


def resolve_reaction(kind: str, emoji: str) -> ReactionAction | None:
    if kind == "update":
        return _UPDATE_MAP.get(emoji)
    if kind == "discovery":
        return _DISCOVERY_MAP.get(emoji)
    return None


def route_reaction(
    *, engine: Engine, message_id: str, emoji: str, now_epoch: int,
    skip_window_weeks: int = 4,
) -> None:
    with session_scope(engine) as s:
        entry = s.execute(
            select(DigestEntry).where(DigestEntry.discord_message_id == message_id)
        ).scalar_one_or_none()
        if entry is None:
            log.debug("reaction on unknown message %s", message_id)
            return
        kind = entry.kind
        thread_id = entry.f95_thread_id

    action = resolve_reaction(kind, emoji)
    if action is None:
        log.debug("ignoring reaction %s on %s message", emoji, kind)
        return

    apply_reaction(
        engine=engine, thread_id=thread_id, kind=kind, action=action,
        now_epoch=now_epoch, skip_window_weeks=skip_window_weeks,
    )
```

- [ ] **Step 4: Run tests, see green**

Run: `uv run pytest tests/unit/test_reactions_router.py -v`
Expected: 6 passed.

- [ ] **Step 5: Commit**

```bash
git add src/vnmaster/bot/reactions.py tests/unit/test_reactions_router.py
git commit -m "feat: emoji-to-action resolver + message-to-thread router"
```

---

### Task 13.3: discord.py bot client wrapper

**Files:**
- Create: `src/vnmaster/bot/client.py`

- [ ] **Step 1: Implement client.py (lightly tested via integration only)**

```python
"""discord.py bot harness.

The on_raw_reaction_add handler is a one-line shim that calls
`route_reaction`. All state logic lives in pure functions tested separately.
"""
from __future__ import annotations

import asyncio

import discord
from discord.ext import commands
from sqlalchemy import Engine

from vnmaster.bot.reactions import route_reaction
from vnmaster.clock import Clock, SystemClock
from vnmaster.logging_setup import get_logger

log = get_logger(__name__)


class VNMasterBot(commands.Bot):
    def __init__(
        self,
        *,
        engine: Engine,
        channel_id: int,
        guild_id: int,
        clock: Clock | None = None,
        skip_window_weeks: int = 4,
    ) -> None:
        intents = discord.Intents.default()
        intents.message_content = True
        intents.reactions = True
        intents.dm_messages = True
        super().__init__(command_prefix="!vnm-unused-", intents=intents)
        self._engine = engine
        self._channel_id = channel_id
        self._guild_id = guild_id
        self._clock = clock or SystemClock()
        self._skip_window_weeks = skip_window_weeks

    async def on_ready(self) -> None:
        log.info("logged in as %s", self.user)

    async def on_raw_reaction_add(
        self, payload: discord.RawReactionActionEvent
    ) -> None:
        if payload.channel_id != self._channel_id:
            return
        if payload.user_id == (self.user.id if self.user else 0):
            return
        emoji = str(payload.emoji)
        route_reaction(
            engine=self._engine,
            message_id=str(payload.message_id),
            emoji=emoji,
            now_epoch=self._clock.now_epoch(),
            skip_window_weeks=self._skip_window_weeks,
        )


def run_bot(*, token: str, engine: Engine, channel_id: int, guild_id: int) -> None:
    bot = VNMasterBot(engine=engine, channel_id=channel_id, guild_id=guild_id)
    asyncio.run(bot.start(token))
```

- [ ] **Step 2: Commit**

```bash
git add src/vnmaster/bot/client.py
git commit -m "feat: VNMasterBot discord.py harness routing reactions"
```

---

## Phase 14 — Slash commands

### Task 14.1: Pure command handlers

**Files:**
- Create: `src/vnmaster/bot/slash.py`
- Create: `tests/unit/test_slash.py`

- [ ] **Step 1: Write the failing test**

`tests/unit/test_slash.py`:
```python
from pathlib import Path

import pytest
from sqlalchemy import select

from vnmaster.bot.slash import (
    InvalidUrlError,
    NoSuchTagError,
    cmd_pair, cmd_status, cmd_tags_add, cmd_tags_list, cmd_tags_remove,
)
from vnmaster.config import Config
from vnmaster.db.engine import create_engine_for, session_scope
from vnmaster.db.models import Base, Pairing


def _engine(tmp_path: Path):
    e = create_engine_for(tmp_path / "v.db")
    Base.metadata.create_all(e)
    return e


def _cfg(tmp_path: Path) -> Config:
    return Config.load(
        Path(__file__).parent.parent / "fixtures" / "configs" / "valid.toml"
    )


def test_tags_add_persists_to_config(tmp_path: Path) -> None:
    cfg_path = tmp_path / "config.toml"
    cfg_path.write_text((Path(__file__).parent.parent / "fixtures" / "configs" / "valid.toml").read_text())
    out = cmd_tags_add(config_path=cfg_path, tag="harem")
    assert "harem" in out
    text = cfg_path.read_text()
    assert '"harem"' in text


def test_tags_remove_strips_from_config(tmp_path: Path) -> None:
    cfg_path = tmp_path / "config.toml"
    cfg_path.write_text((Path(__file__).parent.parent / "fixtures" / "configs" / "valid.toml").read_text())
    cmd_tags_remove(config_path=cfg_path, tag="corruption")
    text = cfg_path.read_text()
    assert '"corruption"' not in text


def test_tags_remove_unknown_raises(tmp_path: Path) -> None:
    cfg_path = tmp_path / "config.toml"
    cfg_path.write_text((Path(__file__).parent.parent / "fixtures" / "configs" / "valid.toml").read_text())
    with pytest.raises(NoSuchTagError):
        cmd_tags_remove(config_path=cfg_path, tag="not-there")


def test_tags_list_returns_current(tmp_path: Path) -> None:
    cfg_path = tmp_path / "config.toml"
    cfg_path.write_text((Path(__file__).parent.parent / "fixtures" / "configs" / "valid.toml").read_text())
    out = cmd_tags_list(config_path=cfg_path)
    assert "corruption" in out
    assert "slow-burn" in out
    assert "ntr" in out  # exclude tag should also show


def test_pair_writes_to_pairings(tmp_path: Path) -> None:
    engine = _engine(tmp_path)
    cmd_pair(
        engine=engine,
        name="SecretSave-9999",
        f95_url="https://f95zone.to/threads/eternum.12345/",
        now_epoch=100,
    )
    with session_scope(engine) as s:
        row = s.execute(select(Pairing)).scalar_one()
        assert row.f95_thread_id == 12345
        assert row.save_dir_name == "SecretSave-9999"


def test_pair_invalid_url_raises(tmp_path: Path) -> None:
    engine = _engine(tmp_path)
    with pytest.raises(InvalidUrlError):
        cmd_pair(engine=engine, name="X", f95_url="https://example.com/", now_epoch=100)


def test_status_reports_counts(tmp_path: Path) -> None:
    engine = _engine(tmp_path)
    out = cmd_status(engine=engine)
    assert "last digest run" in out.lower() or "no digest runs yet" in out.lower()
```

- [ ] **Step 2: Run, see red**

Run: `uv run pytest tests/unit/test_slash.py -v`
Expected: ImportError.

- [ ] **Step 3: Implement slash.py**

```python
"""Slash command handlers (pure functions).

The discord.py command shells live elsewhere; these are the unit-testable
business logic.
"""
from __future__ import annotations

import re
import tomllib
from pathlib import Path

import tomli_w
from sqlalchemy import Engine, func, select

from vnmaster.db.engine import session_scope
from vnmaster.db.models import DigestRun, LibraryGame, Pairing


_F95_THREAD_RE = re.compile(r"f95zone\.to/threads/[^/]*\.(\d+)/?")


class InvalidUrlError(ValueError):
    pass


class NoSuchTagError(ValueError):
    pass


def cmd_tags_add(*, config_path: Path, tag: str) -> str:
    data = tomllib.loads(config_path.read_text())
    data.setdefault("discovery", {}).setdefault("include_tags", [])
    if tag not in data["discovery"]["include_tags"]:
        data["discovery"]["include_tags"].append(tag)
    config_path.write_text(tomli_w.dumps(data))
    return f"Added include tag: {tag!r}"


def cmd_tags_remove(*, config_path: Path, tag: str) -> str:
    data = tomllib.loads(config_path.read_text())
    discovery = data.get("discovery", {})
    in_inc = tag in discovery.get("include_tags", [])
    in_exc = tag in discovery.get("exclude_tags", [])
    if not in_inc and not in_exc:
        raise NoSuchTagError(tag)
    if in_inc:
        discovery["include_tags"].remove(tag)
    if in_exc:
        discovery["exclude_tags"].remove(tag)
    config_path.write_text(tomli_w.dumps(data))
    return f"Removed {tag!r}"


def cmd_tags_list(*, config_path: Path) -> str:
    data = tomllib.loads(config_path.read_text())
    inc = data.get("discovery", {}).get("include_tags", [])
    exc = data.get("discovery", {}).get("exclude_tags", [])
    return (
        f"Include: {', '.join(inc) or '(none)'}\n"
        f"Exclude: {', '.join(exc) or '(none)'}"
    )


def cmd_pair(*, engine: Engine, name: str, f95_url: str, now_epoch: int) -> str:
    m = _F95_THREAD_RE.search(f95_url)
    if not m:
        raise InvalidUrlError(f"{f95_url!r} does not look like an F95 thread URL")
    thread_id = int(m.group(1))
    with session_scope(engine) as s:
        s.merge(Pairing(
            f95_thread_id=thread_id,
            save_dir_name=name,
            confidence=1.0,
            paired_at=now_epoch,
        ))
    return f"Paired {name!r} → thread #{thread_id}"


def cmd_status(*, engine: Engine) -> str:
    with session_scope(engine) as s:
        last = s.execute(
            select(DigestRun).order_by(DigestRun.run_at.desc()).limit(1)
        ).scalar_one_or_none()
        library_count = s.execute(
            select(func.count()).select_from(LibraryGame)
        ).scalar_one()
        unmatched = s.execute(
            select(func.count())
            .select_from(LibraryGame)
            .where(LibraryGame.latest_upstream_version.is_(None))
        ).scalar_one()
    if last is None:
        return f"No digest runs yet. Library size: {library_count}. Unmatched: {unmatched}."
    return (
        f"Last digest run: {last.run_at} epoch · "
        f"updates: {last.updates_count} · discovery: {last.discovery_count} · "
        f"llm spend: ${last.llm_cost_usd:.2f}. "
        f"Library size: {library_count}. Unmatched (no upstream version): {unmatched}."
    )
```

- [ ] **Step 4: Run tests, see green**

Run: `uv run pytest tests/unit/test_slash.py -v`
Expected: 7 passed.

- [ ] **Step 5: Commit**

```bash
git add src/vnmaster/bot/slash.py tests/unit/test_slash.py
git commit -m "feat: slash command handlers for tags, pair, status"
```

---

## Phase 15 — Pipeline orchestration

### Task 15.1: End-to-end pipeline orchestrator

**Files:**
- Create: `src/vnmaster/pipeline.py`
- Create: `tests/integration/test_pipeline_e2e.py`

- [ ] **Step 1: Write the failing test (everything mocked except DB)**

`tests/integration/test_pipeline_e2e.py`:
```python
import json
from pathlib import Path
from unittest.mock import AsyncMock, MagicMock

import pytest
from sqlalchemy import select

from vnmaster.config import Config
from vnmaster.db.engine import create_engine_for, session_scope
from vnmaster.db.models import Base, DigestRun, LibraryGame
from vnmaster.discovery.client import DiscoveryThread
from vnmaster.llm.changelog import ExtractionResult
from vnmaster.pipeline import PipelineDeps, run_digest_pipeline


@pytest.mark.asyncio
async def test_pipeline_end_to_end_creates_digest_run(
    tmp_path: Path, monkeypatch
) -> None:
    monkeypatch.setenv("HOME", str(tmp_path))
    # Build a real vnmaster.db
    db_path = tmp_path / "vnmaster.db"
    engine = create_engine_for(db_path)
    Base.metadata.create_all(engine)

    # Fake F95Checker DB reader returning one game
    f95_db = MagicMock()
    f95_db.check_schema = MagicMock()
    f95_db.iter_all_games = MagicMock(return_value=iter([
        MagicMock(
            id=42, name="Eternum", version="0.7.0", developer="Caribdis",
            engine="Ren'Py", status="ongoing", last_updated=100, changelog="v0.7\n- 800 renders",
            description="Sci-fi", image_url=None, tags=["corruption"],
            executable=None, archived=0, rating=4,
        )
    ]))

    # Empty filesystem scanners → user has nothing installed/played
    play_scanner = MagicMock(return_value=[])
    disk_scanner = MagicMock(return_value=[])

    # Discovery client returns one thread
    discovery_client = MagicMock()
    discovery_client.fetch_latest.return_value = [
        DiscoveryThread(
            id=99, title="Acolyte", developer="Ember", posted_at=200,
            likes=1000, tags=["corruption"], short_description="...",
            image_url=None, version="0.1",
        )
    ]

    # Mock LLM cache (returns regex-shaped output)
    llm_cache = MagicMock()
    llm_cache.extract_for.return_value = ExtractionResult(
        method="llm",
        versions=[{"version": "0.7.0", "renders": 800, "bugfix_only": False,
                   "summary_one_line": "Maya story"}],
        cost_usd=0.005,
    )

    # Webhook + bot mocks
    webhook = AsyncMock()
    webhook.send.return_value = MagicMock(id="m1")
    bot = MagicMock()
    bot.add_reaction = AsyncMock()

    cfg = Config.load(
        Path(__file__).parent.parent / "fixtures" / "configs" / "valid.toml"
    )

    deps = PipelineDeps(
        engine=engine, f95_db=f95_db, scan_play_history=play_scanner,
        scan_disk=disk_scanner, discovery_client=discovery_client,
        llm_cache=llm_cache, webhook=webhook, bot=bot,
        config=cfg, now_epoch=1_000, channel_id="c-1",
    )
    await run_digest_pipeline(deps)

    with session_scope(engine) as s:
        runs = s.execute(select(DigestRun)).scalars().all()
        assert len(runs) == 1
        assert runs[0].llm_cost_usd > 0

        library = s.execute(select(LibraryGame)).scalars().all()
        assert len(library) == 1
        assert library[0].game_title == "Eternum"

    # At least one webhook send happened (kickoff + at least one embed)
    assert webhook.send.await_count >= 2
```

- [ ] **Step 2: Run, see red**

Run: `uv run pytest tests/integration/test_pipeline_e2e.py -v`
Expected: ImportError.

- [ ] **Step 3: Implement pipeline.py**

```python
"""Top-level orchestrator for one `vnmaster digest` run.

Owns the data flow but no behavior: each component is injected so the
pipeline can be tested as a unit.
"""
from __future__ import annotations

import json
from dataclasses import dataclass
from typing import Callable, Protocol

from sqlalchemy import Engine, select

from vnmaster.config import Config
from vnmaster.db.engine import session_scope
from vnmaster.db.models import (
    DigestEntry, DigestRun, DiscoveryState as DiscoveryStateRow,
    DiscoverySuggestion, LibraryGame, Pairing,
)
from vnmaster.db.ro_f95checker import F95CheckerDB
from vnmaster.digest.embeds import build_discovery_embed, build_update_embed
from vnmaster.digest.poster import DiscordPoster
from vnmaster.digest.select import select_digest_candidates
from vnmaster.discovery.client import DiscoveryClient
from vnmaster.discovery.filter import (
    DiscoveryState as FilterState, apply_filter_pipeline, rank,
)
from vnmaster.logging_setup import get_logger
from vnmaster.magnitude import sum_since
from vnmaster.matcher import match_library
from vnmaster.scanners.types import InstalledGame, PlayHistoryEntry

log = get_logger(__name__)


class LLMCache(Protocol):
    def extract_for(self, thread_id: int, raw: str, title: str): ...


@dataclass
class PipelineDeps:
    engine: Engine
    f95_db: F95CheckerDB
    scan_play_history: Callable[..., list[PlayHistoryEntry]]
    scan_disk: Callable[..., list[InstalledGame]]
    discovery_client: DiscoveryClient
    llm_cache: LLMCache
    webhook: object
    bot: object
    config: Config
    now_epoch: int
    channel_id: str


async def run_digest_pipeline(deps: PipelineDeps) -> None:
    cfg = deps.config

    # 1. Schema check + read F95Checker
    deps.f95_db.check_schema()
    f95_rows = list(deps.f95_db.iter_all_games())

    # 2. Scanners
    play_history = deps.scan_play_history(cfg.paths.renpy_saves_root)
    installed = deps.scan_disk(cfg.paths.games_root)

    # 3. Match
    cached_pairings = _load_pairings(deps.engine)
    match_result = match_library(
        play_history=play_history, installed=installed,
        f95_rows=f95_rows, cached_pairings=cached_pairings,
        fuzzy_threshold=cfg.matching.fuzzy_threshold,
    )

    # 4. Upsert library_games
    _upsert_library(deps.engine, match_result, f95_rows, deps.now_epoch)

    # 5. Discovery feed
    threads = deps.discovery_client.fetch_latest(days=cfg.discovery.discovery_lookback_days)
    library_ids = {m.f95_thread_id for m in match_result.matches}
    states = _load_discovery_states(deps.engine)
    filtered = apply_filter_pipeline(
        threads,
        library_thread_ids=library_ids,
        states=states,
        include_tags=cfg.discovery.include_tags,
        exclude_tags=cfg.discovery.exclude_tags,
        now_epoch=deps.now_epoch,
    )
    ranked = rank(filtered, now_epoch=deps.now_epoch,
                  include_tags=cfg.discovery.include_tags)
    ranked = ranked[: cfg.discovery.max_suggestions_per_digest]
    _upsert_discovery(deps.engine, ranked, deps.now_epoch)

    # 6. Select digest candidates
    previous = _previous_run_at(deps.engine)
    candidates = select_digest_candidates(
        engine=deps.engine, previous_digest_run_at=previous,
        now_epoch=deps.now_epoch, max_repeat_weeks=4,
        max_discovery=cfg.discovery.max_suggestions_per_digest,
    )

    # 7. LLM extract per update
    update_embeds = []
    llm_calls = 0
    llm_cost = 0.0
    for u in candidates.updates:
        result = deps.llm_cache.extract_for(
            thread_id=u.f95_thread_id, raw=u.raw_changelog or "", title=u.game_title
        )
        llm_calls += 1
        llm_cost += result.cost_usd
        score = sum_since(result.versions, u.installed_version, cfg.magnitude_score)
        deltas = _aggregate_deltas(result.versions, u.installed_version)
        summary = _pick_summary_line(result.versions, u.installed_version)
        embed = build_update_embed(
            u, deltas=deltas, magnitude_score=score, summary_one_line=summary
        )
        update_embeds.append((u.f95_thread_id, embed))

    # 8. Discovery embeds
    discovery_embeds = []
    include_set = set(cfg.discovery.include_tags)
    for d in candidates.discovery:
        tags = json.loads(d.tags_json or "[]")
        matching = [t for t in tags if t in include_set]
        discovery_embeds.append((d.f95_thread_id, build_discovery_embed(d, matching_tags=matching)))

    # 9. Post
    poster = DiscordPoster(webhook=deps.webhook, bot_client=deps.bot,
                           channel_id=deps.channel_id)
    kickoff = (
        f"Weekly digest — {len(update_embeds)} library updates · "
        f"{len(discovery_embeds)} new games"
    )
    posted = await poster.post(
        kickoff_text=kickoff, update_embeds=update_embeds,
        discovery_embeds=discovery_embeds,
    )

    # 10. Record run + entries
    with session_scope(deps.engine) as s:
        run = DigestRun(
            run_at=deps.now_epoch,
            updates_count=len(update_embeds),
            discovery_count=len(discovery_embeds),
            llm_calls=llm_calls,
            llm_cost_usd=llm_cost,
        )
        s.add(run)
        s.flush()
        for message_id, embed_index, kind, thread_id in posted.entries:
            s.add(DigestEntry(
                run_id=run.id, discord_message_id=message_id,
                embed_index=embed_index, kind=kind, f95_thread_id=thread_id,
            ))
            # stamp last_seen_in_digest_at on library/discovery rows
            if kind == "update":
                s.execute(
                    select(LibraryGame).where(LibraryGame.f95_thread_id == thread_id)
                ).scalar_one().last_seen_in_digest_at = deps.now_epoch
            else:
                s.execute(
                    select(DiscoverySuggestion).where(
                        DiscoverySuggestion.f95_thread_id == thread_id
                    )
                ).scalar_one().last_seen_in_digest_at = deps.now_epoch


def _load_pairings(engine: Engine) -> dict[str, int]:
    out: dict[str, int] = {}
    with session_scope(engine) as s:
        for p in s.execute(select(Pairing)).scalars().all():
            if p.save_dir_name:
                out[p.save_dir_name] = p.f95_thread_id
            if p.folder_name:
                out[p.folder_name] = p.f95_thread_id
    return out


def _load_discovery_states(engine: Engine) -> dict[int, FilterState]:
    out: dict[int, FilterState] = {}
    with session_scope(engine) as s:
        for r in s.execute(select(DiscoveryStateRow)).scalars().all():
            out[r.f95_thread_id] = FilterState(
                hidden=bool(r.hidden), skip_until=r.skip_until
            )
    return out


def _upsert_library(engine, match_result, f95_rows, now_epoch: int) -> None:
    f95_by_id = {f.id: f for f in f95_rows}
    with session_scope(engine) as s:
        for m in match_result.matches:
            f95 = f95_by_id.get(m.f95_thread_id)
            existing = s.execute(
                select(LibraryGame).where(LibraryGame.f95_thread_id == m.f95_thread_id)
            ).scalar_one_or_none()
            data = dict(
                game_title=m.game_title,
                save_dir_name=m.save_dir_name,
                last_played_at=m.last_played_at,
                first_played_at=m.first_played_at,
                save_count=m.save_count,
                total_save_size_bytes=m.total_save_size_bytes,
                install_path=str(m.install_path) if m.install_path else None,
                installed_version=m.installed_version,
                disk_size_bytes=m.disk_size_bytes,
                latest_upstream_version=f95.version if f95 else None,
                upstream_last_updated_at=f95.last_updated if f95 else None,
                upstream_thread_url=(
                    f"https://f95zone.to/threads/.{m.f95_thread_id}/" if f95 else None
                ),
                raw_changelog=f95.changelog if f95 else None,
                tags_json=json.dumps(f95.tags) if f95 else None,
                status=f95.status if f95 else None,
                developer=f95.developer if f95 else None,
                image_url=f95.image_url if f95 else None,
                updated_at=now_epoch,
            )
            if existing is None:
                s.add(LibraryGame(
                    f95_thread_id=m.f95_thread_id, created_at=now_epoch, **data
                ))
            else:
                for k, v in data.items():
                    setattr(existing, k, v)


def _upsert_discovery(engine, threads, now_epoch: int) -> None:
    with session_scope(engine) as s:
        for t in threads:
            existing = s.execute(
                select(DiscoverySuggestion).where(
                    DiscoverySuggestion.f95_thread_id == t.id
                )
            ).scalar_one_or_none()
            data = dict(
                title=t.title, developer=t.developer, posted_at=t.posted_at,
                likes=t.likes, tags_json=json.dumps(t.tags),
                short_description=t.short_description, image_url=t.image_url,
            )
            if existing is None:
                s.add(DiscoverySuggestion(
                    f95_thread_id=t.id, first_seen_at=now_epoch, **data
                ))
            else:
                for k, v in data.items():
                    setattr(existing, k, v)


def _previous_run_at(engine: Engine) -> int:
    with session_scope(engine) as s:
        last = s.execute(
            select(DigestRun).order_by(DigestRun.run_at.desc()).limit(1)
        ).scalar_one_or_none()
        return last.run_at if last else 0


def _aggregate_deltas(versions: list[dict], user_version: str | None) -> dict[str, int]:
    from vnmaster.magnitude import parse_version
    if user_version is None:
        relevant = versions
    else:
        u = parse_version(user_version)
        relevant = [v for v in versions if parse_version(v["version"]) > u]
    out: dict[str, int] = {}
    for key in ("renders", "animations", "words", "scenes",
                "new_locations", "new_characters"):
        total = sum((v.get(key) or 0) for v in relevant)
        if total:
            out[key] = total
    return out


def _pick_summary_line(versions: list[dict], user_version: str | None) -> str:
    from vnmaster.magnitude import parse_version
    if user_version is None:
        relevant = versions
    else:
        u = parse_version(user_version)
        relevant = [v for v in versions if parse_version(v["version"]) > u]
    for v in relevant:
        s = v.get("summary_one_line") or ""
        if s:
            return s
    return ""
```

- [ ] **Step 4: Run tests, see green**

Run: `uv run pytest tests/integration/test_pipeline_e2e.py -v`
Expected: 1 passed.

- [ ] **Step 5: Wire `vnmaster digest` to the pipeline**

Update `src/vnmaster/cli.py`'s `digest` function:
```python
@main.command()
@click.option(
    "--config", "config_path", type=click.Path(exists=True, path_type=Path),
    default=None, help="Path to config.toml",
)
def digest(config_path: Path | None) -> None:
    """Run one digest pipeline now and post to Discord."""
    import asyncio
    from anthropic import Anthropic

    from vnmaster.config import Config, Secrets
    from vnmaster.db.engine import create_engine_for
    from vnmaster.db.ro_f95checker import F95CheckerDB
    from vnmaster.discovery.client import DiscoveryClient
    from vnmaster.llm.budget import InMemoryBudget
    from vnmaster.llm.cache import CachedExtractor
    from vnmaster.llm.changelog import ChangelogExtractor
    from vnmaster.logging_setup import configure_logging
    from vnmaster.paths import VNMasterPaths
    from vnmaster.pipeline import PipelineDeps, run_digest_pipeline
    from vnmaster.scanners.disk import scan_disk
    from vnmaster.scanners.play_history import scan_play_history
    from vnmaster.clock import SystemClock
    import discord
    import time

    paths = VNMasterPaths.defaults_for_macos()
    configure_logging(paths.log_dir / "vnmaster.log")
    cfg = Config.load(config_path or paths.config_dir / "config.toml")
    secrets = Secrets.load(paths.config_dir / "secrets.toml")

    engine = create_engine_for(cfg.paths.vnmaster_db)
    f95_db = F95CheckerDB.open(cfg.paths.f95checker_db)
    anthropic_client = Anthropic(api_key=secrets.anthropic_api_key)
    budget = InMemoryBudget(cap_usd=cfg.anthropic.monthly_budget_usd)
    extractor = ChangelogExtractor(
        client=anthropic_client, model=cfg.anthropic.model, budget_tracker=budget
    )
    cache = CachedExtractor(inner=extractor, engine=engine, clock=lambda: int(time.time()))

    webhook = discord.SyncWebhook.from_url(cfg.discord.webhook_url)
    # Convert sync send to async-shaped for the poster.
    class _WebhookAdapter:
        def __init__(self, w):
            self._w = w
        async def send(self, *args, **kwargs):
            return self._w.send(*args, **kwargs, wait=True)
    bot = discord.Client(intents=discord.Intents.none())

    deps = PipelineDeps(
        engine=engine, f95_db=f95_db, scan_play_history=scan_play_history,
        scan_disk=scan_disk, discovery_client=DiscoveryClient(),
        llm_cache=cache, webhook=_WebhookAdapter(webhook), bot=bot,
        config=cfg, now_epoch=SystemClock().now_epoch(),
        channel_id=cfg.discord.channel_id,
    )

    async def main_async() -> None:
        await bot.login(secrets.discord_bot_token)
        try:
            await run_digest_pipeline(deps)
        finally:
            await bot.close()

    asyncio.run(main_async())
    click.echo("digest run complete")
```

- [ ] **Step 6: Commit**

```bash
git add src/vnmaster/pipeline.py src/vnmaster/cli.py tests/integration/test_pipeline_e2e.py
git commit -m "feat: pipeline orchestrator wiring scanners + matcher + LLM + Discord"
```

---

## Phase 16 — First-run wizard

### Task 16.1: Wizard steps

**Files:**
- Create: `src/vnmaster/init_wizard.py`
- Create: `tests/unit/test_init_wizard.py`

- [ ] **Step 1: Write the failing test**

`tests/unit/test_init_wizard.py`:
```python
from pathlib import Path

from vnmaster.init_wizard import generate_import_candidates


def test_generate_candidates_orders_by_confidence() -> None:
    candidates = [
        ("low", "ObscureGame", "no F95 match", None),
        ("high", "Eternum", "fuzzy 95", "https://f95zone.to/threads/eternum.42/"),
        ("medium", "MyGame", "fuzzy 91", "https://f95zone.to/threads/mygame.43/"),
    ]
    out = generate_import_candidates(candidates)
    lines = out.strip().splitlines()
    # high-confidence first, low-confidence last
    assert lines[0].startswith("# HIGH")
    assert "Eternum" in lines[0]
    assert "low" in out.lower()


def test_blank_candidates_returns_header_only() -> None:
    out = generate_import_candidates([])
    assert "no candidates" in out.lower()
```

- [ ] **Step 2: Run, see red**

Run: `uv run pytest tests/unit/test_init_wizard.py -v`
Expected: ImportError.

- [ ] **Step 3: Implement init_wizard.py**

```python
"""First-run interactive setup.

Wizard steps are described in spec §6. Most steps are I/O (click prompts, HTTP);
the testable bit is the candidate-list generator.
"""
from __future__ import annotations

from typing import Iterable

import click
from sqlalchemy import Engine

from vnmaster.config import Config, Secrets
from vnmaster.db.ro_f95checker import F95CheckerDB
from vnmaster.scanners.disk import scan_disk
from vnmaster.scanners.play_history import scan_play_history


def generate_import_candidates(
    rows: Iterable[tuple[str, str, str, str | None]],
) -> str:
    """Render an import-candidates.txt file body.

    Each row: (confidence_label, game_title, hint, f95_url_or_None)
    Confidence order: high → medium → low.
    """
    order = {"high": 0, "medium": 1, "low": 2}
    sorted_rows = sorted(rows, key=lambda r: order.get(r[0].lower(), 3))
    if not sorted_rows:
        return "# No candidates found.\n"
    lines = []
    for confidence, title, hint, url in sorted_rows:
        prefix = f"# {confidence.upper():6s} | {title} | {hint}"
        lines.append(prefix)
        lines.append(url or "# (no URL — pair manually with /vnm pair)")
        lines.append("")
    return "\n".join(lines)


def run_wizard(paths) -> None:
    """Interactive wizard. Each step shells out to already-tested helpers."""
    import tomli_w
    from anthropic import Anthropic
    from rapidfuzz import fuzz, process

    click.echo("VNMaster setup wizard")
    click.echo("=" * 40)

    # 1. Confirm paths
    games_root = click.prompt("Games folder", default=str(paths.games_root))
    saves_root = click.prompt("Ren'Py saves folder", default=str(paths.renpy_saves_root))
    f95_db_path = click.prompt(
        "F95Checker DB path", default=str(paths.f95checker_db)
    )

    # 2. Verify F95Checker
    if not Path(f95_db_path).exists():
        click.echo(
            f"F95Checker DB not found at {f95_db_path}. Install F95Checker "
            "from https://github.com/WillyJL/F95Checker, add at least one "
            "game, then re-run `vnmaster init`."
        )
        raise SystemExit(1)

    # 3. Anthropic
    anthropic_key = click.prompt("Anthropic API key", hide_input=True)
    test_client = Anthropic(api_key=anthropic_key)
    test_client.messages.create(
        model="claude-haiku-4-5", max_tokens=1,
        messages=[{"role": "user", "content": "hi"}],
    )

    # 4. Discord
    discord_token = click.prompt("Discord bot token", hide_input=True)
    guild_id = click.prompt("Discord guild ID")
    channel_id = click.prompt("Discord channel ID")
    webhook_url = click.prompt("Discord webhook URL")

    # 5. Run scanners
    play = scan_play_history(Path(saves_root))
    installed = scan_disk(Path(games_root))
    click.echo(f"Found {len(play)} played games, {len(installed)} installed.")

    # 6. Generate candidates
    f95 = F95CheckerDB.open(Path(f95_db_path))
    f95.check_schema()
    f95_rows = list(f95.iter_all_games())
    rows = _build_candidate_rows(play, installed, f95_rows)
    candidates_path = paths.config_dir / "import-candidates.txt"
    candidates_path.parent.mkdir(parents=True, exist_ok=True)
    candidates_path.write_text(generate_import_candidates(rows))
    click.echo(f"Import candidates written to {candidates_path}")
    click.confirm("Paste those URLs into F95Checker, then press y to continue", abort=True)

    # 7. Save config.toml (tags collected later via slash command)
    config_data = {
        "paths": {
            "games_root": games_root,
            "renpy_saves_root": saves_root,
            "f95checker_db": f95_db_path,
            "vnmaster_db": str(paths.vnmaster_db),
        },
        "discord": {"guild_id": guild_id, "channel_id": channel_id,
                    "webhook_url": webhook_url},
        "anthropic": {"model": "claude-haiku-4-5", "monthly_budget_usd": 5.0},
        "schedule": {"cron": "0 9 * * SAT"},
        "discovery": {"include_tags": [], "exclude_tags": [],
                      "max_suggestions_per_digest": 10,
                      "skip_window_weeks": 4, "discovery_lookback_days": 7},
        "matching": {"fuzzy_threshold": 90},
        "magnitude_score": {
            "renders": 1.0, "animations": 5.0, "words_per_1k": 1.0,
            "scenes": 50.0, "new_locations": 20.0, "new_characters": 10.0,
            "bugfix_only_penalty": -100.0,
        },
    }
    (paths.config_dir / "config.toml").write_text(tomli_w.dumps(config_data))
    secrets_path = paths.config_dir / "secrets.toml"
    secrets_path.write_text(
        tomli_w.dumps({
            "discord_bot_token": discord_token,
            "anthropic_api_key": anthropic_key,
        })
    )
    secrets_path.chmod(0o600)

    # 8. Install launchd jobs
    from vnmaster.launchd import (
        install_plists, render_bot_plist, render_weekly_plist,
    )
    import shutil
    bin_path = Path(shutil.which("vnmaster") or "vnmaster")
    paths.log_dir.mkdir(parents=True, exist_ok=True)
    weekly = render_weekly_plist(
        bin_path=bin_path, log_dir=paths.log_dir, cron="0 9 * * SAT"
    )
    bot = render_bot_plist(bin_path=bin_path, log_dir=paths.log_dir)
    launchagents = Path.home() / "Library" / "LaunchAgents"
    weekly_path, bot_path = install_plists(
        weekly_text=weekly, bot_text=bot, launchagents_dir=launchagents
    )
    click.echo(f"Installed launchd plists: {weekly_path}, {bot_path}")
    click.echo(
        "Run `launchctl load ~/Library/LaunchAgents/dev.vnmaster.bot.plist` to start the bot, "
        "and `launchctl load ~/Library/LaunchAgents/dev.vnmaster.weekly.plist` for the schedule."
    )

    click.echo(
        "\nSetup complete. Add your tag preferences with `/vnm tags add <tag>` "
        "in your Discord channel after starting the bot, then run "
        "`vnmaster digest` to send your first digest."
    )


def _build_candidate_rows(play_history, installed, f95_rows) -> list[tuple[str, str, str, str | None]]:
    """Cross-reference scanner outputs with F95Checker entries to suggest URLs."""
    from rapidfuzz import fuzz, process

    f95_names = {f.name: f.id for f in f95_rows}
    rows = []
    seen = set()

    def add(name: str, hint: str) -> None:
        if name in seen:
            return
        seen.add(name)
        best = process.extractOne(name, list(f95_names.keys()), scorer=fuzz.token_set_ratio)
        if best is None:
            rows.append(("low", name, hint, None))
            return
        match_name, score, _ = best
        thread_id = f95_names[match_name]
        if score >= 90:
            conf = "high"
        elif score >= 75:
            conf = "medium"
        else:
            conf = "low"
        rows.append(
            (conf, name, f"{hint} (matched {match_name!r}, score {score:.0f})",
             f"https://f95zone.to/threads/.{thread_id}/")
        )

    for p in play_history:
        add(p.save_dir_name, "from ~/Library/RenPy/")
    for i in installed:
        add(i.folder_name, "from ~/Games/")
    return rows
```

- [ ] **Step 4: Run tests, see green**

Run: `uv run pytest tests/unit/test_init_wizard.py -v`
Expected: 2 passed.

- [ ] **Step 5: Wire to CLI**

Update `cli.py`'s `init` function to call `run_wizard(VNMasterPaths.defaults_for_macos())`.

- [ ] **Step 6: Commit**

```bash
git add src/vnmaster/init_wizard.py tests/unit/test_init_wizard.py src/vnmaster/cli.py
git commit -m "feat: first-run wizard skeleton + import candidates generator"
```

---

## Phase 17 — launchd integration

### Task 17.1: Plist templates and installer

**Files:**
- Create: `scripts/plists/dev.vnmaster.weekly.plist.template`
- Create: `scripts/plists/dev.vnmaster.bot.plist.template`
- Create: `src/vnmaster/launchd.py`
- Create: `tests/unit/test_launchd.py`

- [ ] **Step 1: Create plist templates**

`scripts/plists/dev.vnmaster.weekly.plist.template`:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>dev.vnmaster.weekly</string>
  <key>ProgramArguments</key>
  <array>
    <string>{{VNMASTER_BIN}}</string>
    <string>digest</string>
  </array>
  <key>StartCalendarInterval</key>
  <dict>
    <key>Weekday</key>
    <integer>{{WEEKDAY}}</integer>
    <key>Hour</key>
    <integer>{{HOUR}}</integer>
    <key>Minute</key>
    <integer>{{MINUTE}}</integer>
  </dict>
  <key>StandardOutPath</key>
  <string>{{LOG_DIR}}/weekly.out.log</string>
  <key>StandardErrorPath</key>
  <string>{{LOG_DIR}}/weekly.err.log</string>
</dict>
</plist>
```

`scripts/plists/dev.vnmaster.bot.plist.template`:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>dev.vnmaster.bot</string>
  <key>ProgramArguments</key>
  <array>
    <string>{{VNMASTER_BIN}}</string>
    <string>bot</string>
  </array>
  <key>KeepAlive</key>
  <true/>
  <key>RunAtLoad</key>
  <true/>
  <key>StandardOutPath</key>
  <string>{{LOG_DIR}}/bot.out.log</string>
  <key>StandardErrorPath</key>
  <string>{{LOG_DIR}}/bot.err.log</string>
</dict>
</plist>
```

- [ ] **Step 2: Write the failing test**

`tests/unit/test_launchd.py`:
```python
from pathlib import Path

import pytest

from vnmaster.launchd import (
    InvalidCronError, parse_simple_cron, render_weekly_plist, render_bot_plist,
)


def test_parse_cron_extracts_minute_hour_weekday() -> None:
    parsed = parse_simple_cron("0 9 * * SAT")
    assert parsed.minute == 0
    assert parsed.hour == 9
    assert parsed.weekday == 7  # launchd Sunday=0, Saturday=6 — but discord-style uses 7 for SAT in some
    # Actually map SAT→6 (launchd convention).
    # We canonicalize via the parser's assigned mapping in the impl.


def test_parse_cron_rejects_complex_expressions() -> None:
    with pytest.raises(InvalidCronError):
        parse_simple_cron("*/5 * * * *")


def test_render_weekly_plist_substitutes_placeholders(tmp_path: Path) -> None:
    out = render_weekly_plist(
        bin_path=Path("/opt/bin/vnmaster"),
        log_dir=Path("/Users/x/Logs"),
        cron="0 9 * * SAT",
    )
    assert "/opt/bin/vnmaster" in out
    assert "/Users/x/Logs" in out
    assert "<integer>9</integer>" in out
    assert "<integer>0</integer>" in out


def test_render_bot_plist(tmp_path: Path) -> None:
    out = render_bot_plist(
        bin_path=Path("/opt/bin/vnmaster"),
        log_dir=Path("/Users/x/Logs"),
    )
    assert "/opt/bin/vnmaster" in out
    assert "<true/>" in out  # KeepAlive
```

(Adjust the weekday expectation in the test once the impl picks a convention — see step 3.)

- [ ] **Step 3: Run, see red**

Run: `uv run pytest tests/unit/test_launchd.py -v`
Expected: ImportError.

- [ ] **Step 4: Implement launchd.py**

```python
"""Render and install launchd plists for the weekly digest + bot daemon."""
from __future__ import annotations

import re
from dataclasses import dataclass
from pathlib import Path

# launchd Weekday convention: 0 or 7 = Sunday; 1 = Monday; ...; 6 = Saturday.
_WEEKDAY_MAP = {
    "SUN": 0, "MON": 1, "TUE": 2, "WED": 3, "THU": 4, "FRI": 5, "SAT": 6,
}


class InvalidCronError(ValueError):
    pass


@dataclass(frozen=True)
class SimpleCron:
    minute: int
    hour: int
    weekday: int


_CRON_RE = re.compile(
    r"^(?P<minute>\d+)\s+(?P<hour>\d+)\s+\*\s+\*\s+(?P<weekday>[A-Z]{3})$"
)


def parse_simple_cron(expr: str) -> SimpleCron:
    m = _CRON_RE.match(expr.strip())
    if not m:
        raise InvalidCronError(
            "vnmaster supports only `M H * * DAY` cron expressions where DAY is SUN-SAT"
        )
    weekday = _WEEKDAY_MAP.get(m.group("weekday"))
    if weekday is None:
        raise InvalidCronError(f"unknown weekday: {m.group('weekday')!r}")
    return SimpleCron(
        minute=int(m.group("minute")),
        hour=int(m.group("hour")),
        weekday=weekday,
    )


def render_weekly_plist(*, bin_path: Path, log_dir: Path, cron: str) -> str:
    parsed = parse_simple_cron(cron)
    template = (Path(__file__).resolve().parent.parent.parent / "scripts" / "plists"
                / "dev.vnmaster.weekly.plist.template").read_text()
    return (
        template
        .replace("{{VNMASTER_BIN}}", str(bin_path))
        .replace("{{LOG_DIR}}", str(log_dir))
        .replace("{{WEEKDAY}}", str(parsed.weekday))
        .replace("{{HOUR}}", str(parsed.hour))
        .replace("{{MINUTE}}", str(parsed.minute))
    )


def render_bot_plist(*, bin_path: Path, log_dir: Path) -> str:
    template = (Path(__file__).resolve().parent.parent.parent / "scripts" / "plists"
                / "dev.vnmaster.bot.plist.template").read_text()
    return (
        template
        .replace("{{VNMASTER_BIN}}", str(bin_path))
        .replace("{{LOG_DIR}}", str(log_dir))
    )


def install_plists(
    *, weekly_text: str, bot_text: str, launchagents_dir: Path
) -> tuple[Path, Path]:
    launchagents_dir.mkdir(parents=True, exist_ok=True)
    weekly = launchagents_dir / "dev.vnmaster.weekly.plist"
    bot = launchagents_dir / "dev.vnmaster.bot.plist"
    weekly.write_text(weekly_text)
    bot.write_text(bot_text)
    return weekly, bot
```

- [ ] **Step 5: Fix the weekday assertion in the test**

Re-read the test and align with the convention chosen in `_WEEKDAY_MAP` (`SAT → 6`). Adjust to `assert parsed.weekday == 6`.

- [ ] **Step 6: Run tests, see green**

Run: `uv run pytest tests/unit/test_launchd.py -v`
Expected: 4 passed.

- [ ] **Step 7: Commit**

```bash
git add scripts/plists/ src/vnmaster/launchd.py tests/unit/test_launchd.py
git commit -m "feat: launchd plist renderers for weekly job + bot daemon"
```

---

## Phase 18 — Final integration check

### Task 18.1: Manual end-to-end smoke

**Files:** (none — manual procedure recorded in README)

- [ ] **Step 1: Write README.md with the smoke procedure**

`README.md`:
```markdown
# VNMaster

Weekly Discord digest of F95Zone Ren'Py game updates with LLM-extracted
changelog magnitude.

## Install (macOS)

```bash
uv tool install .
```

## First-run

```bash
vnmaster init
```

This wizard:
1. Confirms paths under your home directory.
2. Verifies F95Checker is installed.
3. Prompts for Anthropic API key + Discord credentials.
4. Scans `~/Library/RenPy/` and `~/Games/`.
5. Generates `~/.config/vnmaster/import-candidates.txt`. Review and paste
   URLs into F95Checker's bulk-import.
6. Collects tag preferences.
7. Sends a test digest.
8. Installs launchd jobs.

## Manual smoke test

After `vnmaster init`:

```bash
# Trigger an out-of-schedule digest run.
vnmaster digest --config ~/.config/vnmaster/config.toml

# Check status.
vnmaster status

# Tail logs.
tail -f ~/Library/Logs/VNMaster/vnmaster.log
```

You should see one Discord post in your configured channel with at least
the kickoff message; subsequent embeds appear if the library has games and
the discovery feed returns results.

## Tests

```bash
uv run pytest -q
```

LLM live-API tests run only when explicitly opted in:

```bash
VNMASTER_LIVE_LLM=1 uv run pytest -m live_llm -v
```
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: README with install + first-run + smoke instructions"
```

---

## Summary checklist

After all phases complete, verify the following manually:

- [ ] `uv run pytest -q` is green.
- [ ] `uv run mypy src/vnmaster --strict` is clean (or all `# type: ignore`s have justifications).
- [ ] `uv run ruff check .` is clean.
- [ ] `vnmaster --help` lists all subcommands.
- [ ] `vnmaster init` walks the wizard end-to-end on a clean machine.
- [ ] `vnmaster digest` posts a digest to Discord and writes a `DigestRun` row.
- [ ] `vnmaster bot` starts and connects to the gateway.
- [ ] Reactions in the configured channel update `library_games.hidden`, `interested`, `acknowledged_version`, or `discovery_state.*`.
- [ ] The two launchd plists install and `launchctl list | grep vnmaster` shows both labels.

---

## Plan self-review notes

**Spec coverage** — each spec section maps to at least one task:

| Spec § | Plan coverage |
|--------|---------------|
| §3 Architecture | Phase 15 orchestrator wires the diagram |
| §4.1 play-history scanner | Phase 4 |
| §4.2 disk scanner | Phase 5 |
| §4.3 F95Checker DB reader | Phase 6 |
| §4.4 library matcher | Phase 7 |
| §4.5 discovery fetcher | Phase 10 |
| §4.6 changelog magnitude extractor | Phase 8 (LLM + cache + budget) + Phase 9 (score) |
| §4.7 digest builder | Phase 11 (select + embeds) |
| §4.8 Discord poster | Phase 12 |
| §4.9 reaction bot | Phase 13 (apply + router + client) + Phase 14 (slash) |
| §5 configuration | Phase 2 |
| §6 first-run wizard | Phase 16 |
| §7 testing strategy | TDD enforced per task; live-LLM gated in Phase 8.2 |
| §8 risks | schema check (Phase 6), budget cap (Phase 8.3), regex fallback (Phase 8.1) |
| §9 deps | pyproject.toml in Phase 1.1 |
| §10 user prereqs | README + wizard prompts in Phase 16 + 18 |

**Open implementer-judgment items** (intentional):

- The wizard's interactive flow uses `click.prompt`; the implementer may swap to a TUI library if they prefer, but the order of steps is fixed.
- The `import-candidates.txt` format is human-readable text. If F95Checker's bulk-import accepts a structured format better, the implementer may switch.
- Discord embed pagination beyond 25 entries: spec mentions splitting; the plan's `DiscordPoster` posts one embed per message, so the 10-embeds-per-message limit doesn't apply. If the user's library produces 100+ updates, the implementer should add a sanity cap (e.g., 30 updates max per digest) and surface a "+N more" footer.

**Known type-consistency notes:**

- `DiscoveryState` is the name of both an ORM model (Phase 3) and a filter dataclass (Phase 10). Phase 15's orchestrator imports them with aliases (`DiscoveryStateRow`, `FilterState`) to disambiguate.
- `parse_version` is exposed as a public helper in `magnitude.py` (renamed from the early-draft `_parse_version`) so the pipeline can use it without a private-import smell.

**Things I deliberately did NOT plan:**

- No tests for `bot/client.py` (the discord.py harness). All testable logic lives in the pure functions it calls (`route_reaction`, `apply_reaction`). The harness itself is wiring.
- No "browser-extension auto-add" feature for the ✅ discovery reaction. Per spec §4.5, the baseline is "bot DMs you the URL"; auto-add is a v1.1 candidate.
- No mid-week instant alerts. Spec §11 defers to v1.1.

